# FCONV-T23-WIRE — T2/T3 tool_use qua hybrid+fconv trên instance test (2026-08-26)

## VERDICT: T2 PASS + T3 PASS — SẴN SÀNG FLIP

Tiếp nối `fconv-e2e-wire-rerun-2026-08-26.md` (T1 PASS). Cả hai mức tool_use đều PASS
ở lần chạy đầu (retry budget dùng: 0/2). Đường fconv (prepare chain → authed envelope,
không browser fallback) chạy thật xuyên suốt multi-turn tool loop.

## Setup (đúng quy trình rerun)

- `cp -a ~/.local/share/webgpt/profiles/personal ~/Downloads/webgpt-e2e-t23-profile`,
  xoá 3 symlink `Singleton{Lock,Cookie,Socket}` trong bản copy.
- Instance TEST :18001, pid 3312956:

  ```
  WEBGPT_FCONV_PREPARE=1 WEBGPT_RUNTIME_ROOT=/tmp/e2e-fconv-runtime2 \
    .venv/bin/python -m gpt.debug api-server --port 18001 --transport hybrid --headless \
    --profile-dir /home/light/Downloads/webgpt-e2e-t23-profile --allow-authenticated \
    --trace-file /tmp/e2e-fconv-runtime2/tmp/fconv-t23-trace.jsonl
  ```

- Health check trước test: `/health` = 200; process env xác nhận
  `WEBGPT_FCONV_PREPARE=1`, `WEBGPT_RUNTIME_ROOT=/tmp/e2e-fconv-runtime2`.
- Production :18000 + unit `webgpt-gateway.service`: không đụng, còn sống sau test.

## T2 — single-step tool_use exact-match (budget 1/2)

`.venv/bin/python scripts/verify_hybrid_flip.py --base-url http://127.0.0.1:18001 --level t2 --timeout 120`

```
[T2] PASS (84.346s)
http_status=200, rounds_used=1, tool_use_count=1
stop_reasons=["tool_use","end_turn"], loop_error=null
file_content_verbatim="HYBRID_FLIP_T2_OK", content_match=true
conversation_id=6a8ec586-e604-83ec-af4e-48cb6b622a3b
```

Corr count gateway (trace): submit 1 có `correction_count=2`
(`MALFORMED_TOOL`/protocol_shaped idx1 → `FALSE_COMPLETION`/general idx2, chạm
effective_cap=2 nhưng **recover trong cùng request**, submit_completed assistant_chars=274);
submit 2 `correction_count=0`, finish `stop`.

## T3 — mini-loop nhiều turn tool_use (chạy vì T2 PASS)

`.venv/bin/python scripts/verify_hybrid_flip.py --base-url http://127.0.0.1:18001 --level t3 --timeout 120`

```
[T3] PASS (63.590s)
http_status=200, rounds_used=2, tool_use_count=2 (cap 10, hit_round_cap=false)
stop_reasons=["tool_use","tool_use","end_turn"], loop_error=null
script_stdout_verbatim="SUM=338350"  (expected SUM=338350)
script_runs_cleanly=true, result_file_has_expected_line=true
conversation_id=6a8ec5e9-ad0c-83ec-a263-34c594ff5846
```

Corr count gateway: turn 1 `correction_count=1` (`MALFORMED_TOOL`/protocol_shaped idx1,
recover cùng request); turn 2 và 3 `correction_count=0`.

## Budget turn live

T2 = 2 request HTTP + T3 = 3 request HTTP = **5/15 turn**. Không level nào cần retry.

## Evidence đường fconv

1. **Env**: `/proc/3312956/environ` có `WEBGPT_FCONV_PREPARE=1`; không có
   `WEBGPT_CODEX_SSE` (process env lẫn `.env`) → tại `curl_transport.py`
   `fconv = (not codex) and fconv_prepare_enabled()` = True.
2. **Không fallback**: hybrid transport không có đường browser cho send;
   `_prepare_fconv_turn` (proof → chat-requirements → PoW → POST
   `/backend-api/f/conversation/prepare`) là điều kiện bắt buộc trước MỌI POST —
   5/5 submit_completed đều đã qua prepare chain.
3. **Trace** `/tmp/e2e-fconv-runtime2/tmp/fconv-t23-trace.jsonl` (59 events): chuỗi đầy đủ
   `request_start → lease_acquired → prompt_built → pending_marked → position_* →
   submit_start → [tool_correction*] → submit_completed → parsed → request_completed`
   x 5 request; **zero** event `AuthRequired`/`submit_failed`. Turn 2+ của mỗi conversation
   thấy `position_skipped_affinity_hit` — affinity hội thoại hoạt động đúng.
4. **Token cache** `<profile-copy>/webgpt-token-cache.json` (mode 0600): cookie `oai-did`
   prefix `84ab67f2-7cd…` khớp `oai_device_id` `84ab67f2-7cd7-40b9-b11f-…58d884`.
5. **Latency**: `browser_ms` mỗi submit 7.0–70.2s — hợp lý cho prepare chain + SSE authed,
   khác fail tức thì (<2s) khi pre-flight chết như lần trước.

## Quan sát (không blocking)

- Tool-correction (soft `<cmd>` handshake) làm việc thật: mỗi conversation đầu cần 1–2
  lần sửa `MALFORMED_TOOL`; các turn sau sạch corr=0 → protocol ổn định theo session.
- T2 turn 1 chạm cap correction=2 rồi vẫn hoàn tất — biên cap hiện tại đủ nhưng sát;
  nếu sau này tăng task phức tạp có thể xem xét cap=3.
- Verify script ghi scratch vào runtime root mặc định khi shell gọi script không có
  `WEBGPT_RUNTIME_ROOT` (đã dọn 2 workdir); nếu muốn tách hẳn thì export env khi chạy harness.
- Vẫn còn khuyến nghị từ report trước: log INFO một dòng khi `_prepare_fconv_turn`
  hoàn tất để có evidence dương tính trực tiếp.

## Dọn dẹp

- Instance :18001 kill (pid 3312956) — port closed, không còn process.
- Đã xoá: `~/Downloads/webgpt-e2e-t23-profile`, `/tmp/e2e-fconv-runtime2`,
  2 workdir harness `~/.local/share/webgpt/tmp/verify_hybrid_flip_20260826-{175251,175431}`.
- Giữ lại: `~/Downloads/webgpt-e2e-t23-gateway.log` (stdout uvicorn, user tự xoá).
- Không sửa code, không commit, không đụng :18000/unit (health 200, active sau test).

## Kết luận flip

T1 (text streaming) + T2 (tool_use exact-match) + T3 (multi-turn tool loop) đều xanh
trên wire fconv/hybrid với account thật. Không còn blocker kỹ thuật nào ghi nhận:
flip production = đổi unit `webgpt-gateway.service` sang `--transport hybrid`
(giữ `WEBGPT_TOOL_PROTOCOL=soft`, thêm `WEBGPT_FCONV_PREPARE=1`), thực hiện ngoài phạm vi report này.
