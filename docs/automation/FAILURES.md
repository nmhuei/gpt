# FAILURES — các hướng đã thử và thất bại

- [2026-08-24] Sentinel finalize body `{"p": <prepare_token>}` → server trả 500. ĐÃ SỬA thành `{"prepare_token": …}` (200). Không dùng lại shape `p`.
- [2026-08-24] Conduit `/f/conversation/prepare` trả 422 với mọi payload shape đã thử ({}, có/không conversation_id, parent_message_id, backend-api lẫn backend-anon) — schema chuẩn CHƯA BIẾT. Cấm code mù quanh conduit; cần capture body thật (export XML Burp hoặc Repeater).
- [2026-08-24] Direct POST `/backend-api/f/conversation` nhận SSE bị 403 "Unusual activity" khi session bị backend coi noauth — KHÔNG kết luận được SSE chết từ probe này. Probe lại trên phiên đăng nhập tươi đang chạy (W-PROBE).
- [2026-08-24] Agent research lần 1 dừng sớm không tra cứu gì (hiểu nhầm prompt); đã đẩy chạy lại. Bài học: kiểm tra tool_uses > 0 trong kết quả agent research.

- [2026-08-24] Đường AUTHENTICATED (/backend-api/f/conversation) trả 403 'Unusual activity' KỂ CẢ khi đủ proof+turnstile+requirements từ SentinelSDK — nguyên nhân là reputation của thiết bị/IP (persona cứ chatgpt-noauth). KHÔNG mất thời gian retry mù đường này; đường anon đã xác minh sống. Browser transport vẫn là đường authenticated chính thức.
  > **CẬP NHẬT 2026-08-25 (RESEARCH-HYBRID-AUTH bác bỏ):** 403 KHÔNG phải reputation IP/thiết bị — cùng máy/IP, trang thật POST → 200 SSE còn curl_cffi đủ sentinel → 403 ⇒ **request-shape-differentiated**: thiếu conduit handshake (x-conduit-token + /f/conversation/prepare, prepare_conduit là dead code), sec-ch-ua trio, oai-session-id, telemetry headers, cookie jar đầy đủ (oai-sc/__cf_bm/_puid), body ~15 field. Lối ra chứng minh cộng đồng: POST /backend-api/codex/responses không gate Turnstile → ROADMAP CODEX-SSE.

- [2026-08-24] Chạy full pytest trong lúc nhiều agent đang edit repo → pytest blocked vô hạn (0% CPU, kẹt 29'). Quy tắc mới: chỉ chạy full suite giữa các wave; trong wave dùng targeted test theo cụm file.
- [2026-08-24] Agent nền chờ background task tự thoát vòng lặp 'đang chờ' 3 lần không báo cáo → TaskStop + tiếp quản thủ công. Bài học: cấm agent dùng run_in_background cho verify; bắt chúng chạy foreground và báo cáo nguyên văn.

- [2026-08-24] HƯỚNG CHẾT: nhúng tool protocol vào message body (dù format gì: XML/JSON/bootstrap block) — injection-defense của ChatGPT classify là untrusted content, model từ chối 23/24 response (VERIFY-R4). Mọi fix tầng prompt vô hiệu với gốc này. Chuyển hướng: (a) framing thấp-key không banner; (b) Custom GPT / Project instructions làm trusted context. Nguồn: docs/reports/live-cli-verify-round4-2026-08-24.md

- [2026-08-24] VERIFY-R6 đo trên gateway chạy code STALE (process 17:27 < file mtime 18:39, không ai restart sau merge) → kết quả phản ánh hành vi cũ. Quy tắc bắt buộc: SAU MỖI lần merge fix phải `systemctl --user restart webgpt-gateway` TRƯỚC khi dispatch verify live; verify agent cũng phải check process start-time vs file mtime đầu tiên.

- [2026-08-25] Script không main-guard tự bắn live: probe `--help` của verify agent V3 vô tình THỰC THI body của run_claude_ctf_task.py / run_claude_misc_task.py / solve_ctf_with_files.py (không argparse, không __main__ guard, hardcode task cũ) → script tự chạy claude bắn POST /v1/messages vào gateway ~30s trước khi bị giết, đốt quota và vi phạm tinh thần single-flight. Quy tắc: (1) mọi verify agent PHẢI đọc đầu script kiểm tra argparse/main-guard TRƯỚC khi gọi --help; (2) cần bổ sung main-guard cho cả 3 script này (backlog).

- [2026-08-25] NOTIFICATION BỊ TRỘN KHI CONTEXT NÉN: completion notification của R4-DOUBLING/V5/HYBRID-CACHE/MED về từ sớm nhưng bị lẫn trong đống context crash-recovery → coordinator lần theo STATE.md stale, tưởng 4 agent vẫn chạy trong khi thực tế xong từ lâu; phát hiện muộn nhờ owner nhắc "check thực tế". Quy tắc bắt buộc từ giờ: ĐẦU MỖI TICK phải reconcile trạng thái bằng DISK ARTIFACTS (report file tồn tại? grep symbol đặc trưng trong code? test file mới?) thay vì chỉ tin danh sách in-flight nội bộ — disk là nguồn sự thật.

- [2026-08-24] VERIFY-R6 đo trên gateway chạy code STALE (process 17:27 < file mtime 18:39, không ai restart sau merge) → kết quả phản ánh hành vi cũ. Quy tắc bắt buộc: SAU MỖI lần merge fix phải `systemctl --user restart webgpt-gateway` TRƯỚC khi dispatch verify live; verify agent cũng phải check process start-time vs file mtime đầu tiên.

- [2026-08-26] Agent chết vì API transient (upstream_read_error) có notification nhưng bị LỘT trong context replay sau compaction — FIX-A/B tưởng đang chạy 65' trong khi chết từ trước; hôm trước cũng lọt FIX-C/OPS-SYNC ngay khi dispatch. Quy tắc: (1) mỗi tick đối chiếu từng agent với artifact-on-disk (report file mtime / workspace activity); agent không report >30' và không artifact → coi nghi chết; (2) khi resume agent chết, BẮT buộc chỉ thị "kiểm tra git diff/grep đĩa trước khi sửa tiếp" để tránh apply trùng.

- [2026-08-26] `accounts/accounts.json` BỊ XOÁ khỏi ~/Downloads/webgpt/accounts giữa ~11:12-11:14 ⇒ gateway crash-loop 'Unknown account profile: personal' (94 lần respawn) cho tới khi MED-BATCH4 agent tái tạo registry từ schema (đúng DEFAULT_WEBGPT_ROOT=~/Downloads/webgpt, chmod 0600, không mất secret vì credentials_file vốn null, profile personal nguyên vẹn). Nghi phạm CHƯA chốt: full pytest #2 chạy ngay 11:10-11:12 trước giờ G (tests AccountStore dùng tmp_path riêng nhưng test mới test_preflight_quota.py/test_debug_login cần rà env-scrub); code có hàm destructive unlink/rmtree (accounts.py:234-256) chưa rõ caller. VIỆC PHÒNG THỦ: ROADMAP ACCOUNTS-REGISTRY-RESILIENCE — backup-on-write registry + startup check cảnh báo nếu thiếu + audit caller hàm xoá.
- [2026-08-26 bổ sung] Kết quả truy thủ phạm accounts.json: KHÔNG tìm thấy trong repo — duy nhất một caller destructive là `gpt/debug.py:300` (CLI thủ công `account remove --delete-profile`, ghi đè registry chứ không xoá file); mọi rmtree/unlink khác nhắm tmp/sandbox; 3 test nghi vấn đều isolate đúng. Kết luận làm việc: thao tác ngoài source tree (nghi người/việc lạ trên máy); đã có phòng tuyến .bak + warn. Việc nhỏ còn lại: conftest scrub thêm WEBGPT_ACCOUNTS_FILE/WEBGPT_PROFILES_ROOT.
- [2026-08-26 GIẢI MÃ CUỐI] Thủ phạm xoá accounts.json + nguyên nhân "rate-limit" cả chiều: OWNER lỡ xoá/di chuyển ~/Downloads/webgpt (DEFAULT_WEBGPT_ROOT chứa registry + profile browser với cookie đăng nhập). Toàn bộ dấu hiệu RateLimited/commit_unknown chiều nay thực chất là MẤT PHIÊN ĐĂNG NHẤP bị classify nhầm (session.py:269-277 map auth_status required/blocked → RateLimited với message 'anonymous quota exhausted' — SAI cho account đã login). Bài học kép: (1) classify auth-vs-quota phải tách bạch (LIMIT-SIGNATURE chỉ vá đường curl, đường browser vẫn nuốt chung); (2) trước khi kết luận upstream-quota phải kiểm tra auth_status/profile thật. Owner sẽ tự đăng nhập lại.
