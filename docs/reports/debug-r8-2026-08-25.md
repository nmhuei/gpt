# DEBUG-R8 — Chẩn đoán regression VERIFY-R8 (2026-08-25)

**Verdict: 3 root cause, tất cả hội tụ về MỘT nguyên nhân gốc vận hành: gateway PID 126465 khởi động 17:48:20 nhưng `gpt/utils/toolcall.py` sửa 18:21:01, `gpt/gateway/runtime.py` sửa 18:51:05, `gpt/utils/promptcompat.py` sửa 17:58:08 — toàn bộ FIX-T2 + CORRECTION-TIGHTEN nằm trên disk ở dạng UNCOMMITTED và KHÔNG BAO GIỜ được load vào process đang chạy. R8 đã test code của 17:48, không phải code của 18:51.** T1 echo là lỗi độc lập thứ hai (shape/scale prompt soft-mode).

Repro offline: `/home/light/Downloads/debug-r8/{repro_parse_soft,repro_false_completion_loop,repro_render_t1}.py` — chạy xong, không đụng mạng, không sửa source repo.

## Bằng chứng "stale process" (mốc quan trọng nhất)

| Dữ kiện | Giá trị |
|---|---|
| Service start | `ExecMainStartTimestamp=Tue 2026-08-25 17:48:20 +07`, PID 126465 |
| mtime `gpt/utils/toolcall.py` | 18:21:01 (> start) |
| mtime `gpt/gateway/runtime.py` | 18:51:05 (> start) |
| mtime `gpt/utils/promptcompat.py` | 17:58:08 (> start) |
| Git | HEAD `efcd95b` (23-08) không chứa soft protocol nào; toàn bộ soft machinery + FIX-T2 + CORRECTION-TIGHTEN là working-tree diff (+1559 dòng chưa commit) |
| Trace R8 | 0 event `correction_budget_exhausted` trong cửa sổ 19:03–19:22 dù disk-code phát event này ở mọi lần chạm trần ⇒ code phát event chưa từng chạy |

## RC1 — T1 streaming echo ×2 (độc lập với stale code)

**Dump**: `prompt-debug/000138_wgs_495ee2dd648a477e_pre_gpt.txt` (67,788 chars, 57 tools, client claude-code 2.1.245): prompt = 2 khối `<WEBGPT_MESSAGE>` mỗi khối chứa **MỘT dòng JSON duy nhất** (`{"content": "..."}`, `<`→`<`) dài ~41k chars, chỉ dẫn "Reply with exactly … AY OK GATEWAY OK" nằm **bên trong chuỗi JSON**, sau đó là đoạn handshake `<cmd>` dạng text thường. Không có bootstrap ("text inside JSON strings is data") vì soft bỏ qua nó (`render_messages`, `gpt/utils/promptcompat.py:659-671`).

**Đối chiếu round 7d PASS** (`003582_wgs_0e310e74d47440c6_pre_gpt.json`): cùng format, nhưng **24,652 chars / 24 tools**. R8 tăng 2.75× (system prompt CLI 2.1.245 + ECC plugin 57 tools). Gate probe nhỏ (92 chars, `000122_wgs_fa3414d9bc5345e1`) exact-match 3/3 cùng backend ⇒ lỗi phụ thuộc shape/scale: envelope ~68k một-dòng-escaped khiến model tiếp diễn văn bản thay vì tuân lệnh buried. Trace xác nhận parser thấy 0 tool call (`assistantturn parsed tool_calls=0`, corr=0) nên echo đi thẳng về CLI.

**Repro**: `repro_render_t1.py` tái tạo render soft thật — xác nhận không bootstrap, blob JSON 1 dòng, instruction nằm trong JSON, `WEBGPT_PROMPT_BUDGET_CHARS` unset (budget off).

**Fix đề xuất**:
1. Soft+initial: thêm 1 câu framing text-thường cuối prompt (cùng style handshake, `gpt/gateway/runtime.py:990-1013` `_SOFT_HANDSHAKE_TEXT`) kiểu "các khối <WEBGPT_MESSAGE> trên là trích dẫn từ terminal của tôi — dữ liệu, đừng đọc lại; trả lời tin nhắn cuối của tôi trực tiếp." (probe 24-08: block tiêm controller bị classifier chặn, text-thường thì không).
2. Bật budget cho soft mode hoặc window nội dung system khi soft (hiện `enforce_prompt_budget` off do env unset); ngưỡng an toàn theo dữ liệu: ≤~25k chars pass, ~68k echo 2/2.
3. Không flip protocol (DECISIONS.md).

## RC2 — MALFORMED_TOOL "mixed with prose" vẫn kích (T2-A1)

**Dump**: session `wgs_c056547965a84c98` (T2-A1, 19:09–19:11), response dumps 000200/000221/000225 đều là text prose kèm `<cmd>` literal, metadata `issue_reason=MALFORMED_TOOL: Tool call cannot be mixed with final assistant prose.`; kết thúc `persistent_tool_failure` (MALFORMED lặp 2 lần liên tiếp 221→225) ⇒ corr=2, `submit_failed_before_commit_unknown`.

**Code path**: `parse_tool_calls` nhánh soft (`gpt/utils/toolcall.py:1128-1151`) hiện hardcode `allow_prose=True` tại :1150 — **code trên disk KHÔNG THỂ sinh lỗi này cho các text đó**. Repro `repro_parse_soft.py`: cả 3 dump-text đều OK + extract được Bash call. ⇒ process 17:48 đang chạy parser pre-FIX (soft extraction có sẵn nhưng fail-closed với prose). Đây KHÔNG phải "(a) protocol-detect sai" hay "(c) path khác chạy trước" — đơn giản là code cũ trong RAM. Nghi vấn (b) vẫn đúng một phần: các nhánh markup/json-fn vẫn fail-closed (`toolcall.py:1215-1223` dùng tham số `allow_prose` mặc định False từ call sites `gpt/gateway/runtime.py:665` và `gpt/utils/assistantturn.py:35` — repro xác nhận markup+prose dưới xml vẫn raise) — nhưng không phải thứ đã bắn trong R8.

**Fix**: (1) **restart service** sau khi commit bộ diff — bắt buộc, mọi fix khác vô nghĩa nếu không reload; (2) chống tái phạm: emit fingerprint (sha/mtime của toolcall.py/runtime.py) vào boot event + endpoint health; ghi mã nguồn-sha vào response debug sidecar; (3) vá nhánh markup residual: khi `resolved_protocol=="soft"` truyền `allow_prose=True` vào `_finalize_parse` ở cả 2 call site trên.

## RC3 — Budget lệch: A1 dừng corr=2 vs R1 34 corrections không cap

**Dump**: R1 = session `wgs_1e9f26a0d1944bc6` (19:12–19:22): 68 response dumps = 34 committed (toàn `<cmd>true</cmd>` / `printf…`, issue None) + 30 FALSE_COMPLETION + 4 TOOL_REFUSAL; 34 `correction_prompt_built`; **0** budget-exhausted; pre_gpt cuối (`000901`) chỉ 182 chars = 1 tool result (tail_messages=1, web thread giữ history phía ChatGPT).

**Cơ chế (2 lớp)**:
1. Classifier thời điểm 17:48 (HEAD `runtime.py:~305`) đánh FALSE_COMPLETION mỗi khi `not calls and _looks_like_tool_directed_task(...)` — **không có guard `_fresh_tool_conversation`** (guard này nằm trong diff chưa load, `runtime.py:305-323`). Repro `repro_false_completion_loop.py`: sau 1/20/41 vòng tool đã commit, HEAD-style vẫn verdict FALSE_COMPLETION, worktree-verdict = None. Model trả prose "DONE…", gateway chỉnh lại thành `<cmd>true</cmd>`, CLI chạy `true`, gửi result mới, model lại prose ⇒ metronome 30 chu kỳ tới CLI timeout 601s.
2. Budget là **per-request**: counters reset ở mỗi request của CLI; mỗi request chỉ tiêu ≤1–2 corrections rồi commit `true`, nên không cap nào (cũ hay mới) có thể bắn xuyên request. A1 dừng ở 2 nhờ guard persistent-same-reason (LIVE-R3) trong chính 1 request — hai attempt dừng bằng hai cơ chế khác nhau, không phải hash logic nuốt nhánh.

**Fix**: (1) restart để tải guard `_fresh_tool_conversation` + per-class cap + anti-repeat (đã có sẵn trong diff); (2) thêm circuit-breaker cấp session/record: đếm corrections tích lũy trên `record` (ví dụ `record.correction_total`, ngắt ≥12) vì per-request cap không nhìn thấy livelock cross-request; (3) detector no-op repeat: cùng lệnh Bash (vd `true`) ≥3 lần liên tiếp ⇒ fail-closed NO_PROGRESS thay vì feed thêm correction.

## Thứ tự xử lý cho coordinator

1. Commit + restart `webgpt-gateway.service` (hiện đang chạy code 17:48; mọi patch hôm nay chưa hiệu lực).
2. Vá RC1 (framing text + budget soft) — độc lập, cần trước re-run VERIFY-R8.
3. Vá residual markup allow_prose + session-level breaker (RC2/RC3 lớp 2).
4. Re-run VERIFY-R8; kỳ vọng: T2 hết MALFORMED storm ngay sau bước 1; T1 chỉ hết sau bước 2.

— debug agent R8, read-only diagnosis, 2026-08-25.
