# VERIFY-R8 — Live CLI Parity Verify (2026-08-25)

**Verdict tổng: FAIL — REGRESSION XÁC NHẬN ở T1 (backend/model echo) và T2 (MALFORMED_TOOL correction loop). T3 KHÔNG chạy theo stop-rule.**

## Điều kiện & môi trường

- Gateway: systemd `webgpt-gateway.service`, PID 126465, start **17:48:20**, transport **`browser`**
  (sau rollback hybrid), `WEBGPT_TOOL_PROTOCOL=soft`, `WEBGPT_MAX_CORRECTIONS=4`,
  `WEBGPT_MAX_PROMPT_CHARS=250000`. Không restart, không đụng code.
- CLI: `claude` 2.1.245 (`/home/light/.local/bin/claude`), `--output-format stream-json --verbose`,
  model `claude-sonnet-4-20250514`, env `ANTHROPIC_BASE_URL=http://127.0.0.1:18000`,
  `--dangerously-skip-permissions`. Pattern theo `scripts/run_claude_*.py`.
- Trace: `/home/light/Downloads/webgpt/logs/trace.jsonl` (đã có `correction_count` trong
  `request_completed`/submit events — instrumentation vá xác nhận sống).
- Anchor thời gian trace: monotonic_ns mốc boot ≈ 17:22:56 (uptime 6190.61s @ 19:06:06).

## Gate probe

| Probe | Giờ | Kết quả |
|---|---|---|
| #1 | 19:00:32 | OK 38.7s, resp nguyên văn `GATE_OK_R8` |
| #2 | 19:02:43 | OK 10.4s, resp nguyên văn `GATE_OK_R8` ⇒ **GATE_OPEN** |

Probe = POST trực tiếp `/v1/messages`, prompt nhỏ, không tools, không system → phản hồi exact-match.

Co-tenant: thấy đúng 1 request lạ trước gate — seq 82, client `claude-code`, ok,
dur=45149ms, hoàn tất ≈ 18:34:55 (trước GATE_OPEN ~28 phút), khớp mô tả "probe backend
riêng ≤2 request của agent khác". **Không gặp rate-limit/429 nào trong toàn bộ cửa sổ R8.**

## Bảng kết quả

| Mức | Kết quả | Số liệu nguyên văn |
|---|---|---|
| **T1 streaming đơn giản** | **FAIL ×2** (attempt + 1 retry) | A1: start 19:03:07, rc=0, wall 44s, request_completed dur=40788ms corr=0 tools=0; output_tokens=16779 — model **echo lại toàn bộ envelope** `<WEBGPT_MESSAGE role="system">…</WEBGPT_MESSAGE>` + soft-handshake thay vì trả `AY OK GATEWAY OK`; delta_count=2. R1: 19:06:47, rc=0, dur_ms=10829, corr=0, output_tokens=17023 — echo y hệt |
| **T2 tool_use 1 bước** | **FAIL ×2** | A1: 19:09:31, rc=0, wall 102s, num_turns=4; file `marker_r8.txt` nội dung **exact-match YES** (qua Bash printf ×2); nhưng final text = `[webgpt-gateway:api_error] Persistent MALFORMED_TOOL after correction: Tool call cannot be mixed with final assistant prose.`; trace: 6× `correction_prompt_built`, kết thúc `submit_failed_before_commit_unknown` với **corr=2** (budget MALFORMED≤2 chạm trần). R1: 19:12:26, rc=124 timeout 601s; file `verify_r8_b.txt` đúng nội dung tạo lúc 19:13; sau đó loop **41 Bash tool_use** (37× lệnh rỗng `true`), 41 requests, **34× correction_prompt_built**, 0 submit_failed, không bao giờ kết thúc turn |
| **T3 đa bước tự chủ mini** | **SKIPPED (stop-rule)** | 2/2 mức đầu đỏ thuộc 2 lớp khác nhau ⇒ theo quy trình "vẫn đỏ → dừng, ĐỪNG cày tiếp"; không đốt thêm quota web |

Post-check sau khi dừng: probe nhỏ trực tiếp **OK 10.1s exact `GATE_OK_R8`** ⇒ backend
vẫn healthy với prompt nhỏ; lỗi chỉ xảy ra trên request dạng CLI (57 tools + system
envelope ~16.6k input tokens + soft handshake).

## Chẩn đoán lớp

1. **T1 — lớp backend/model (generation echo)**: transport sạch (mỗi attempt đúng 1 request,
   HTTP ok, 0 failover), parse sạch (text round-trip đủ), **correction không hề được kích hoạt
   (corr=0)** dù output vô dụng cho mục đích — model coi prompt lớn dạng envelope là văn bản
   cần tiếp diễn và regurgitate ~16.8–17k token. Gate probe cùng backend, cùng lúc đó, prompt
   nhỏ → exact compliance ⇒ lỗi phụ thuộc hình thái prompt (size/tools/handshake), không phải
   outage.
2. **T2 — lớp parse/correction**: chuỗi lặp `MALFORMED_TOOL: "Tool call cannot be mixed with
   final assistant prose"`. Chuỗi lỗi này cho thấy check strict prose+tool-mixing **vẫn còn
   kích hoạt** ở đường browser-transport bất chấp FIX-T2 allow_prose soft-protocol — nghi vấn
   regression/tương tác xấu giữa CORRECTION-TIGHTEN (MALFORMED≤2) và FIX-T2. Thêm anomaly:
   A1 chạm budget corr=2 rồi dừng phát lỗi persistent, còn R1 phóng **34 correction builds**
   trong 1 turn mà không cap — enforcement budget không nhất quán giữa 2 lần.
3. **Transport**: sạch hoàn toàn — 0 http_50x, 0 failover, mọi submit ok.
4. **Rate/quota**: không gặp; co-tenant chỉ 1 request trước gate.

## Đối chiếu merge hôm nay (giả thuyết, chưa kết luận)

- Echo T1 ↔ thay đổi khung prompt/handshake (SOFT-COMPACT persistence policy không còn ngăn
  được echo như round 7d ngày 24-08 nơi T1 pass exact).
- MALFORMED storm T2 ↔ CORRECTION-TIGHTEN + FIX-T2 allow_prose: allow_prose chưa hiệu lực
  trên nhánh browser, hoặc detector phân loại nhầm tool-call kèm prose thành MALFORMED.

## Khuyến nghị bước tiếp theo (cho controller)

1. So sánh dump `prompt-debug` session T1 (`wgs_495ee2dd…`, file
   `000138_*_pre_gpt.txt`) với dump round 7d để tìm diff framing/handshake.
2. Trace nhánh phân loại MALFORMED_TOOL trên browser transport: vì sao allow_prose không
   áp dụng; vì sao cap corr lệch nhau giữa 2 attempt (2 vs 34).
3. Re-run VERIFY-R8 sau fix; T3 vẫn chưa có dữ liệu R8.

— Báo cáo bởi verify agent R8, 19:25 +0700 2026-08-25.
