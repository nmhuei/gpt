# VERIFY-R10 — Full Ladder sau reboot (2026-08-26)

**Verdict tổng (FINAL, chốt theo chỉ thị coordinator): PARTIAL — trạng thái BLOCKED-upstream-rate-limit đã NHẢ sau ~15 phút. Gate mở lúc 07:49:45 với 1 probe T1 duy nhất trả HTTP 200 OK (journal uvicorn nguyên văn); breaker half-open pass và đóng. T2/T3 KHÔNG chạy (coordinator cấm thêm probe/retry) ⇒ ladder đầy đủ chưa hoàn tất. Coordinator có thể bắn batch CTF nhưng nên kèm 1 gate probe xác nhận ngay trước batch.**

> Lịch sử verdict trong phiên: FAIL/blocked (07:34–07:47, upstream rate-limit liên tục, xem mục 2) → PARTIAL (07:49, upstream nhả hạn mức).

## Điều kiện & môi trường

- Gateway: systemd `webgpt-gateway.service`, PID **1350**, start **07:19:03** (+07, hôm nay),
  transport **browser**, port 18000, `--account personal --max-workers 8 --warm-workers 4`.
  Không restart, không sửa code, không flip transport/env trong suốt phiên verify.
- Thang: `scripts/verify_hybrid_flip.py --level t1/t2/t3` (đã đọc source trước khi chạy:
  có argparse + main-guard `if __name__ == "__main__"`; **script KHÔNG hỗ trợ gate marker**
  kiểu GATE_OK_R9 ⇒ gate probe làm thủ công bằng POST trực tiếp `/v1/messages` như R9,
  kỳ vọng nguyên văn `GATE_OK_R10`). Base URL ghim `http://127.0.0.1:18000`.
- Trace: `/home/light/Downloads/webgpt/logs/trace.jsonl`.

## 1. Stale-check

| Mốc | Giá trị |
|---|---|
| Process start-time | Wed Aug 26 **07:19:03** 2026 (PID 1350) |
| mtime mới nhất `gpt/**/*.py` | **2026-08-25 21:42:18** (+07) — `gpt/gateway/runtime.py` |
| Kết luận | Code mới nhất **cũ hơn** start-time ~9h37m ⇒ gateway nạp đúng working-tree. **PASS** |

Health tại lúc bắt đầu: `{"ok":true,...,"browser":"not_started",...}` → sau các probe: `"browser":"ready"`. Boot sạch, Uvicorn lên 07:19:04.

## 2. Gate probe — FAIL

Journal từ boot đến 07:34 **không có bất kỳ POST `/v1/messages` nào** (chỉ health poll 200 mỗi 5 phút)
⇒ không co-tenant tranh chấp trong cửa sổ verify.

Chuỗi sự kiện nguyên văn (giờ +07):

| # | Giờ | Kết quả | Evidence |
|---|---|---|---|
| GATE#1 | gửi 07:34:12, trả 07:34:31 (19.5s) | **503** | Turn đầu sau reboot chạm backend → **rate-limit ngay**. Trace seq 5: `status:error, http_status:500→mapped 503`; journal: `BackendCoolingDown: backend cooling down after rate limit (retry allowed in 89.9s)` ⇒ breaker **trip #1** (cửa sổ 90s, deadline ≈07:36:01) |
| GATE#2 | 07:34:31 (0.2s) | **500** | Fail nhanh do breaker vừa mở. Body: `{"type":"error","error":{"type":"api_error","message":"backend cooling down after rate limit (retry allowed in 56.2s)"}}` |
| retry (trong cửa sổ) | 07:35:05 (0.2s) | **500** | Cùng cửa sổ cooldown — retry premature, body `…(retry allowed in 56.2s)` |
| half-open probe | gửi 07:37:22, trả 07:37:38 (15.7s) | **503** | Trace seq 9 nguyên văn: `kind:"conversation_failover", metadata:{"reason":"rate_limited","error_type":"RateLimited"}` ⇒ **trip #2**, penalty ×2 = 180s (deadline ≈07:40:44). Client nhận: `Conversation failed over to a fresh ChatGPT web session; please resend this request to continue.` |
| resend theo chỉ dẫn | 07:38:39 (0.1s) | **500** | `backend cooling down after rate limit (retry allowed in 124.2s)` — vẫn trong cửa sổ 180s |
| half-open probe cuối | gửi 07:43:11, trả 07:43:27 (15.8s) | **503** | Trace seq 14 nguyên văn: lần nữa `conversation_failover reason:"rate_limited" error_type:"RateLimited"` ⇒ **trip #3**, penalty 360s (deadline ≈07:49:27) |

**Kết luận gate:** 3/3 turn backend thật sau reboot đều bị upstream rate-limit. Gateway tự xử đúng thiết kế
(breaker mở cooldown, failover sang session web mới, không resend 228-lần kiểu forensics 08-25) nhưng
**upstream chặn cứng tài khoản `personal` kể từ turn đầu tiên** — khả năng cao là hạn mức theo tài khoản/IP
còn nóng từ phiên dùng heavy hôm trước (R9 + CTF), không phải lỗi sinh ra sau reboot.

Tổng request vào gateway trong phiên verify: 7 POST (3 turn thật + 4 fast-fail qua breaker), HTTP lỗi toàn bộ,
`correction_count=0` mọi record, không request nào hoàn thành.

## 3. Ladder T1/T2/T3 — KHÔNG CHẠY ĐƯỢC (blocked)

Gate FAIL sau khi hết ngân sách retry hợp lệ (initial + 1 retry + 1 half-open probe sau khi hệ thống
báo hết cửa sổ). Mọi request mới sẽ fast-fail 500 qua breaker trong khi mỗi half-open probe chạm
backend đều trip tiếp và nhân đôi cooldown (90→180→360s, trần 600s). Chạy T1/T2/T3 trong trạng thái
này chỉ đốt quota upstream và kéo dài penalty ⇒ **dừng đúng luật, không ép chạy**.

So sánh với R9 (tham chiếu, không đối chiếu trực tiếp được):

| Mức | R9 (25/08) | R10 (26/08) |
|---|---|---|
| T1 streaming | PASS, delta_count=6, first_token **13.473s** | n/a — không chạy |
| T2 tool_use 1 bước | PASS, exact `HYBRID_FLIP_T2_OK`, corr=0 | n/a — không chạy |
| T3 mini-loop ≤10 | PASS, rounds_used=4, SUM=338350 | n/a — không chạy |

## 4. Kết cục theo chỉ thị coordinator (07:47–07:49)

- Coordinator chỉ thị: đợi đủ thời điểm half-open kế tiếp, gửi ĐÚNG 1 probe T1 nhỏ nhất,
  nếu vẫn rate-limit/503 thì chốt `BLOCKED-upstream-rate-limit`, không retry thêm.
- Đợi foreground đến 07:49:45 (sau deadline breaker ≈07:49:27 của trip #3) rồi gửi đúng 1
  turn T1 streaming (`Count from 1 to 20…`, max_tokens 256).
- **Kết quả: HTTP 200 OK** — journal nguyên văn:
  `Aug 26 07:49:45 Huei python[1350]: INFO: 127.0.0.1:39470 - "POST /v1/messages HTTP/1.1" 200 OK`
  (access log uvicorn in sau khi response hoàn tất). Trace seq 16 `request_start`
  `req_2d823c684d72`; row `request_completed` chưa flush vào trace.jsonl tại lúc chốt report.
  Breaker half-open probe thành công ⇒ breaker **closed**, penalty reset.
- Lưu ý minh bạch: metrics phía client (delta_count, first-token latency) KHÔNG có — script
  probe của verify agent lỗi cú pháp parser (`NameError: json` thiếu import) sau khi stream đã
  chạy; đây là lỗi harness agent, không phải gateway. Số liệu client-style sẽ đến từ ladder
  đầy đủ ở lần verify kế.
- **T2/T3: KHÔNG chạy** — tuân thủ cap "đúng 1 việc nữa rồi chốt, không retry thêm".

Trạng thái tài khoản phía ChatGPT Web trong phiên: rate-limit liên tục 07:34:31 → ~07:49:27
(3 trip, penalty 90→180→360s), sau đó tự nhả đúng chu kỳ ngắn. Không phải code gateway.

## 5. Khuyến nghị cho coordinator

1. Batch CTF live có thể cân nhắc bắn lại từ bây giờ (gate đang mở, breaker closed), nhưng
   nên mở đầu bằng 1 gate probe đơn lẻ ngay trước batch để bắt kịp trường hợp hạn mức quay lại.
2. Nếu gate probe fail với rate-limit: dừng hẳn batch, đợi tối thiểu một cửa sổ cooldown
   (bắt đầu 90s, backoff ×2, trần 600s) giữa các lần thử — mỗi probe fail làm gấp đôi cooldown.
3. Không cần đụng gateway/code: stale-check PASS, breaker + failover vận hành đúng thiết kế
   (docs/reports/trace-forensics-2026-08-25.md). Toàn bộ sự cố phiên này nằm ở hạn mức upstream.

— Báo cáo bởi verify agent R10, chốt 07:52 +0700 2026-08-26.
