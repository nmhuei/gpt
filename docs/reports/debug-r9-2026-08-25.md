# debug-r9 — T2 fail: model "xin phép" thay vì emit `<cmd>` (2026-08-25)

Session chứng cứ: `wgs_cc322123c8f5415b` (verify_hybrid_flip_20260825-195319, sau restart 19:52).
Repro offline: `~/Downloads/debug-r9/repro_r9.py` (`.venv/bin/python`, không network, không sửa source).
Kết luận ngắn: **model KHÔNG chờ cấp phép vì hiểu sai handshake** — nó đã emit đúng `<cmd>` ngay lượt 1 và
file đã được tạo ĐÚNG nội dung. Verify FAIL do 2 bug phía gateway kéo session vào vòng correction vô nghĩa
đến khi harness chạm trần `max_rounds=3`.

## 1. Timeline thật (trace.jsonl + prompt-debug dumps)

| POST | seq (trace) | Input web | Reply web | Parse | Hệ quả |
|---|---|---|---|---|---|
| 1 | 3–17 | task + soft handshake (1031 chars) | `<cmd>mkdir…printf…</cmd>` (180 chars) | `tool_calls=1` | harness chạy Bash → **exit=0**, `t2_hello.txt` được tạo ĐÚNG `HYBRID_FLIP_T2_OK\n` (18 bytes, còn nằm trên đĩa) |
| 2 | 19–42 | WEBGPT_TOOL_RESULT exit=0 (179 chars) | prose xác nhận hoàn thành (167 chars): "Verified: … was created…" | `FALSE_COMPLETION` (seq32) | correction #1 gửi 19:53 (262 chars, **task_context_chars=0**) |
| 2' | 33–42 | correction | ack tiếng Việt 208 chars, **trích dẫn nguyên văn** `dạng <cmd>...</cmd>`, nói "không có lệnh nào cần chạy", hỏi lại hướng tiếp theo | `tool_calls=1` (seq41!) | parser nhặt tag quote → `Bash(command="...")` → harness chạy `bash -c ...` → **exit=127 "…: command not found"** (dump 000045) |
| 3 | 44–59 | WEBGPT_TOOL_RESULT exit=127 (217 chars) | 84 chars, `content_chars=0`, `tool_calls=1` (seq58) | ok | harness thực thi → `rounds_used=3 == max_rounds` → `hit_round_cap=True` → **T2 FAIL** |

Dump 000033_response (272 bytes hiển thị cho người gọi) chính là reply 2' — model hiểu protocol, nhưng
(a) bị scold oan là "described the action", (b) không còn gì để chạy, nên trả lời kiểu xin hướng. Không phải
nguyên nhân gốc; đó là TRIỆU CHỨNG của correction false positive.

## 2. Chấm giả thuyết (theo brief)

- (1) Handshake đọc thành "chỉ cmd khi được hỏi" — **BÁC**: T1 emit `<cmd>` ngay (`soft_handshake_appended=true`, seq16 `tool_calls=1`).
- (2) Discover-first khuyến khích hỏi ngược — **BÁC** với session này: `counter_question=False`, dump correction 000033 không có dòng discovery; các biến thể DISCOVER-FIRST/SYSTEM REQUIREMENT/_invoke_batch_guidance chỉ thuộc đường controller "loud" (xml/json-fn), không xuất hiện trong soft path.
- (3) Prompt T2 thiếu ngữ cảnh — **BÁC**: mệnh lệnh rõ, model hành động ngay T1.
- (4) Instruction biến thành content — **BÁC**: handshake là trailing plain text cạnh user message và đã landing ở T1.

## 3. Root cause (xếp hạng)

### BUG A (chính) — FALSE_COMPLETION false positive: `gpt/gateway/runtime.py`
- Nhánh `runtime.py:749-750` (`not calls and _looks_like_tool_directed_task(...)`) **thiếu guard `_fresh_tool_conversation(messages, tail)`** mà nhánh layer-3 kề trên (`:736-739`) có sẵn. Guard này trả False ngay khi transcript có assistant.tool_calls/tool result — tức sẽ chặn đúng case này.
- Bên trong `_looks_like_tool_directed_task`: check "explicitly requested - called_names" (`~:492-503`) chạy **TRƯỚC** exemption "đã có tool result trong tail" (`~:528`). Task chữ "Use the write_file tool" ⇒ `explicitly_requested={write_file}`; soft protocol chỉ dạy `<cmd>` ⇒ `called_names={bash}` mãi mãi ⇒ diff khác rỗng ⇒ luôn True kể cả khi Bash đã tạo xong file. (TODO surface-aware tại `:989` thừa nhận handshake chỉ dạy shell.)
- Reply "Verified: …was created…" cũng không match `_ACTION_CLAIM_MARKERS` (`:267-294`, chỉ có "successfully created"/"has been created and"…) nên không rơi vào nhánh có guard.

### BUG B (trực tiếp gây exit=127) — parser nhặt tag quote: `gpt/utils/toolcall.py`
- `_CMD_TAG_RE` (`:502`) match mọi `<cmd>…</cmd>` trong text; body chỉ bị từ chối khi rỗng (`:550-552`). Body `"..."` (placeholder mà model trích dẫn khi xác nhận format) ⇒ tool call thật `Bash(command="...")`.
- Đây là residual của họ bug "T2 root fix 2026-08-25" đã vá hôm nay (mixed shapes, thiếu Bash surface, prose tolerance) nhưng chưa xử lý placeholder/quote.

### Aggravator — correction mất task context
- `runtime.py:1680` `_original_task_context(prompt_messages)` với `prompt_messages = tail` khi `web_bootstrapped` (`:1503`); tail chỉ có tool result ⇒ `task_context_chars=0` (seq32/33) dù R5-FIX thiết kế "always embed". Model bị phạt mà không được nhắc task ⇒ hợp lý khi đáp "không có lệnh nào cần chạy".

## 4. Fix đề xuất (file:line)

1. `gpt/utils/toolcall.py:549-553` — sau `command = match.group(1).strip()`, thêm blacklist placeholder: body chỉ gồm `.`/`…`/`…`-variants hoặc nằm trong {"...", "the exact shell command", "<command>", "your command"} ⇒ `raise MalformedToolCall("Soft <cmd> contains a placeholder, not a command")` (fail-closed, đồng bộ style hiện có).
2. `gpt/gateway/runtime.py:749` — thêm điều kiện `and _fresh_tool_conversation(messages, tail)` cho nhánh FALSE_COMPLETION generic (guard đã tồn tại, chỉ chưa nối).
3. `gpt/gateway/runtime.py:_looks_like_tool_directed_task` — chuyển exemption "tail có tool result ⇒ False" (~:528) lên **trước** check explicitly-requested (~:502); hoặc trừ `shell_name` ra khỏi demanded-set khi một shell tool đã được gọi (equivalence write_file≈Bash cho mục đích "đã action").
4. `gpt/gateway/runtime.py:1680` — đổi thành `_original_task_context(messages)` (full history, không phụ thuộc bootstrap) để correction luôn mang task như thiết kế R5/LIVE-R3.
5. Follow-up (TODO `:989`): handshake surface-aware — khi surface có write_file-type tool, dạy thêm biến thể `<json>`; bỏ hẳn áp lực "task bắt buộc write_file" dưới protocol chỉ-có-shell.

## 5. Cách tái hiện

```bash
.venv/bin/python /home/light/Downloads/debug-r9/repro_r9.py
```

Output chuẩn (lưu `~/Downloads/debug-r9/repro_r9.out`): prompt T1 render 1031 chars == dump 000003;
reply 208 chars ⇒ `Bash(command="...")`; `_tool_correction_issue("Verified: …")` ⇒
`('FALSE_COMPLETION', 'task requires a controller tool but model returned only prose')` == metadata
`000020_..._response.json`; `_fresh_tool_conversation=False` chứng tỏ guard có sẵn nhưng không được dùng.
