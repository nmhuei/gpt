# ROADMAP — task registry

| ID | Task | Track | Status |
|---|---|---|---|
| C1 | Tách config env-per-terminal | config | done (2026-08-24) |
| C2 | Parser SSE delta_encoding v1 | transport | done (2026-08-24) |
| C3 | Sentinel prepare→finalize + fallback + prepare_conduit | transport | done (2026-08-24) |
| G0 | Sửa shape finalize `{"prepare_token":…}` | transport | done (2026-08-24) |
| W-T1 | Sentinel TTL cache | transport | done (2026-08-24) |
| W-B1B2B4 | Deadline vòng lặp + log exception + solve-slot semaphore | orchestrator | done (2026-08-24) |
| W-P1P3 | Poll env + persist async + TTL evict + cap history | gateway-perf | done (2026-08-24); persist_async chờ wire ở wave 2 |
| WAVE2 | Leak-fix server.py + hook default/health vào lease + wire invalidate_sentinel + fix flaky review-gate tmpdir | wave 2 | done (2026-08-24, 416 pass ×2); persist_async wire chuyển wave 3 |
| W3-PERSIST | Persist nền trong conversations.py | wave 3 | done (2026-08-24): coalesced background flush, mark_pending giữ sync-durable, close() flush cuối; suite 446×2 xanh |
| WATCHLIST | test_cooperative_cancel* nhạy timing khi chạy song song nhiều suite — đã thấy 3 fail transient một lần, sạch khi chạy riêng | QA | done (2026-08-25): marker flaky_timing + retry nội bộ 2 lượt (WATCHLIST-FLAKY agent); 3 lần chạy liên tiếp sạch |
| A3 | Failover an toàn qua re-bootstrap (failover.py + account_health.py wire gateway/server.py:65-514,932) | multi-account | done (xác minh verify V3/V4 2026-08-25; health loop env-gated mặc định OFF) — gộp từ 2 row trùng trái ngược |
| W-A1A4A2 | Health tracker + default account + cooldown 429 | multi-account | done (2026-08-24); hook resolve_default_account vào server hoàn tất 2026-08-25 bởi P2-server (MultiAccountWorkerFactory + lease pinning + health loop lifespan) |
| W-PROBE | Probe SSE phiên đăng nhập tươi → quyết định T3A/T3B | transport | done (2026-08-24): kết luận SSE_GATED_NEED_POW_TURNSTILE — thiếu proof+turnstile header; persona vẫn noauth dù login tươi |
| AUTH-FIX | Sửa 3 bug AutoLoginManager | auth | done (2026-08-24): SSO exclusion + nav-verify + MFA poll 60s; 13 test mới; suite 576 pass |
| LIVE-F2 | T3 root cause: correction loop không bắn khi model trả prose giả | parity | done (2026-08-24): marker chỉ tiếng Anh là nguyên nhân; thêm layer claim-based đa ngữ ("đã tạo/đã chạy") + VN action verbs; suite 662 pass |
| VERIFY-R2 | Re-run T1-T4 sau loạt fix | parity | done (2026-08-24): T1 PASS lần 1 (hết crash/hang); correction bắn đúng (fail-honest); T2 thoái bộ TOOL_REFUSAL cạn budget; T3 vẫn đỏ MALFORMED/FALSE_COMPLETION; docs/reports/live-cli-verify-round2-2026-08-24.md |
| R3-FIX | Corrections=4 + Thinking strip browser-path (glue case ^Thinking(?=[A-Z0-9])) + correction prompt SYSTEM REQUIREMENT | parity | done (2026-08-24); phát hiện: raw response chưa từng được lưu → đã bật prompt-debug+trace-file trên unit; xác nhận SDK retry giữa stream NHÂN ĐÔI generation |
| R4-DOUBLING | Chặn Anthropic SDK retry mid-stream khi gateway đánh dấu non-retryable | parity | done (2026-08-25): mid-stream lỗi → đóng stream chuẩn content_block_stop→message_delta(end_turn)→message_stop kèm lý do dạng text `[webgpt-gateway:<type>]` (block mới hoặc nối block mở); pre-stream giữ 400 + envelope + x-should-retry:false; sửa CẢ HAI api/server.py + gateway/server.py (prod chạy gateway); +4 test, clusters 20/22/8 pass |
| POISON-FIX | Worker poisoning: CancelledError khỏi `except Exception` trong session.send → state kẹt GENERATING vĩnh viễn + factory vẫn coi reusable; RACE deadline↔correction: stream_deadline 330s < worst-case loop ~600s | reliability | done (2026-08-25): send() bắt BaseException → terminal FATAL_ERROR + re-raise CancelledError đúng chuẩn (session.py:509-533); factory whitelist `_REUSABLE_RELEASE_STATES` + lease classify-by-state kể cả BaseException (factory.py:20-32,265-283); deadline floor derive tại entrypoint default 330s→810s budget 4, env override giữ nguyên. Tests 24 passed + liền kề 37/23 |
| RAM-TOP5 | Giam RAM dài hạn: hybrid `_events` queue unbound · ConversationStore transcripts + memo deep-copy 256 bản · trace/prompt-debug disk tăng vô hạn (history_cache session.py DEFER sang wave sau — tranh chấp vùng với POISON-FIX) | hạ tầng | done (2026-08-27 reconcile): bounded hybrid event queue/history memo + trace/debug caps implemented; tests/test_ram_caps.py + test_history_cache_cap.py; full suite 1316 pass |
| TRANSPORT-MODE | systemd unit chạy `--transport hybrid` + WEBGPT_FCONV_PREPARE=1 (FLIPPED 2026-08-26 ~19:40, T1/T2 PASS ngay; backup browser tại *.bak-browser-20260826) — đúng "Protocol over DOM" | ops | FLIP THỬ 17:42 → T1 PASS, T2 FAIL ×2 (kể cả sau rollback!) → ROLLBACK browser. DEBUG-T2 chốt ROOT CAUSE KHÔNG phải transport: soft-protocol parse chết khi model mix prose + <cmd> map "Bash" ảo ngoài allowed_tools (toolcall.py:1066/:529) — chi tiết docs/reports/debug-t2-2026-08-25.md. FIX-T2 đang vá; flip lại SAU khi fix + RESEARCH-HYBRID-AUTH kết luận đường authenticated |
| HYBRID-AUTH-PATH | Làm hybrid hoạt động cho account authenticated | ops | TIN VÀNG từ WS-STREAM research (docs/reports/ws-stream-research-2026-08-25.md): stream authed VẪN là SSE trên f/conversation (Codex Desktop inspect 8/2026 dùng prepare→SSE+resume; WS chỉ lifecycle); recipe HOÀN CHỈNH từ gptweb2api sống 200 ngày: sentinel/chat-requirements/prepare {"p":bootstrapProof} → PoW SHA3-512 → proof gAAAAAB → f/conversation/prepare **15-field body** → conduit_token → SSE + x-conduit-token. Turnstile hiện KHÔNG enforced. Effort port ~1.5-2 ngày |
| PORT-F-CONV-RECIPE | Port recipe gptweb2api vào token_manager/curl_transport (prepare→PoW→conduit_token→SSE) | parity | done (2026-08-26): f/conversation prepare→PoW→conduit→SSE implemented and production hybrid flip certified T1/T2/T3 |
| RECIPE-EXTRACT | Trích nguyên văn mọi field/header/body từ source gptweb2api thành spec triển khai | ops | done/superseded (2026-08-26): recipe extracted, replayed, then implemented by FCONV-NOTOKEN-REPLAY/FCONV production path |
| CONDUIT-PROBE | Replay 1 lần recipe header-diff lên f/conversation authed để chốt số phận đường chính | ops | done (2026-08-25, retry sau API-error): prepare 422 **đòi body bắt buộc** (schema đổi, conduit_token không lấy được) · 2 conv POST 403 application/json ⇒ f/conversation authed CHẾT cho pure HTTP kể cả near-full shape; nghi x-conduit-token + stream đã chuyển WS. Kết luận: hybrid chính thống vô đường — đi CODEX-SSE (blocked token layer) hoặc giữ browser. docs/reports/conduit-probe-2026-08-25.md |
| CODEX-SSE | Nhánh mới hybrid transport qua POST /backend-api/codex/responses — KHÔNG gate Turnstile, chỉ TLS chrome + Bearer + originator:codex_cli_rs + OpenAI-Beta responses=experimental (cộng đồng chứng minh sống với Plus, 02/2026) | parity | IMPLEMENT DONE + LIVEPROBE: nhánh merge (14 passed, default OFF) nhưng liveprobe 2 POST → **401 Unauthorized** — AT web-session bị codex backend từ chối làm credential (endpoint xuyên CF OK — xác nhận thắng no-sentinel của spec; spec sai điểm TTL ~1h, AT thực ~10 ngày). Nghi: cần token OAuth riêng Codex CLI (auth.json/PKCE) hoặc account chưa enroll. RESEARCH-CODEX-OAUTH đang chạy chốt hướng |
| VERIFY-R3 | Live T1-T3 với observability | parity | done (2026-08-24): T1 PASS sạch 16s; root cause structural = correction prompt thiếu task context (tail_messages:0 + session rỗng); 2-POST doubling xác nhận (37 gen/5 turn); docs/reports/live-cli-verify-round3-2026-08-24.md |
| R5-FIX | ORIGINAL USER TASK trong mọi correction prompt + response dump + raise-sớm persistent (2 sends) | parity | done (2026-08-24): test budget cũ cập nhật theo policy mới; suite 665 pass (system py) |
| GATEWAY-CFG | Cấu hình protocol trên unit gateway | ops | ĐÍNH CHÍNH (2026-08-25 verify V2/V3): unit chạy `WEBGPT_TOOL_PROTOCOL=soft` — soft là chuẩn đã chứng minh T5/T6; "both" chưa từng live, giữ soft làm canonical |
| VERIFY-R4 | Vòng đo hội tụ: JSON protocol + task-context correction + discover-first + budget 4 | parity | superseded (2026-08-27 reconcile): later R10/R11 and production post-flip T1/T2/T3 certification provide newer evidence |
| SENTINEL-SDK | Mint proof-token + turnstile-token trong page context | transport | done (2026-08-24): tìm thấy globalThis.SentinelSDK (sdk.js), token('chatgpt') → {p,t,c}; **anon SSE 200 với full sentinel**; auth path 403 do reputation IP/thiết bị |
| T-SENTINEL-WIRE | Wire SentinelSDK mint vào token_manager + 2 header proof/turnstile vào curl_transport (flag WEBGPT_SENTINEL_SDK) | transport | done (2026-08-24, tiếp quản từ agent treo; targeted tests xanh, full verify chờ ranh giới wave) |
| INFRA | Watchdog timer + auto-review timer + AUTOMATION_OPS doc | hạ tầng | done (2026-08-26 reconcile): watchdog timer active trong unit (restart tự phục vụ nhiều lần hôm nay), AUTOMATION_OPS hoàn thiện qua 2 đợt sync — không còn việc mở của row này |
| RGATE | review_gate.py pytest+ruff+danger-scan | QA gate | done (2026-08-24); verify=False hạ medium theo DECISIONS; gate FAIL tạm do test của agent Perf đang sửa dở |
| P2-server | Leak fix `_conversation_locks`/`_response_sessions` + hook health/default vào `_lease_session` + wire invalidate_sentinel vào curl_transport | wave 2 | done (2026-08-25): locks waiter-count eviction; response_sessions LRU cap 512 (WEBGPT_RESPONSE_SESSION_CAP); multi-account wiring; invalidate_sentinel xác nhận có sẵn từ WAVE2 — tests 42/43/59/95 pass |
| B3-full | Cooperative cancel subprocess (SIGTERM→kill) | orchestrator | done (2026-08-24): killer-watchdog SIGTERM grace 5s→SIGKILL, wire 3 điểm chờ, 9 test mới; suite 446×2 xanh |
| P2-affinity | Worker affinity (bỏ open() navigate mỗi turn miss, 2-6s/miss) | gateway-perf | done (2026-08-24): factory+runtime+server passthrough end-to-end, flag WEBGPT_WORKER_AFFINITY; suite 453 pass |
| T4 | Persist TokenBundle + lazy browser + flip hybrid default | transport | done phần persist (T4-PERSIST 2026-08-24); wire cache_dir vào hybrid đang chạy bởi HYBRID-CACHE (2026-08-25) |
| E2E-BENCH | Benchmark E2E: claude CLI dựng project chuyên nghiệp qua gateway (assertion artifact + pytest trong project) | QA | done (2026-08-25): **PASS** — textkit (slugify NFKD chuẩn nghề + extract_emails), pyproject PEP 621, 6 test/6 assert thật/0 trivial, mutation check ăn; 50 turn · 49 tool_use · 26 tool error tự sửa ≥3 vòng · wall 436s · prompt 1.3k chars. Report docs/reports/e2e-bench-2026-08-25.md — mốc verify thực tế mới: model TỰ DỰNG project nghề qua web |
| SOAK | Kế hoạch + harness endurance | QA | done harness (31 test + dry-run sạch); CHỜ DUYỆT chạy thật: hạn mức turn/ngày, khung giờ 03:33, kịch bản recovery kill browser |
| PARITY-AUDIT | Ma trận parity Anthropic↔gateway ~30 feature | parity | done (2026-08-24): docs/reports/api-parity-audit-2026-08-24.md; P0×2, P1×8 — recheck 2026-08-25 (verify V2): parity ~78% chức năng, P1-9 đã fix, chi tiết docs/reports/verify-fromscratch-2026-08-25/parity-claims.md |
| PARITY-P0-1 | Usage tokens ước lượng (chars÷4) — CLI auto-compact hoạt động lại | parity | done (2026-08-26 reconcile coordinator): wire HOÀN TẤT cả 2 protocol qua OPENAI-USAGE-WIRE — gateway/server.py truyền prompt_text đủ call site (:1203, :1382, :1402-1449, stream :1521-1697 estimator.snapshot()); gpt/gateway/adapters.py đã xoá (git status D). Row stale từ 2026-08-25, nghiệm thu bằng grep thật |
| PARITY-P0-2 | Phân lớp phát hiện refusal + raise sớm khi cạn correction | parity | done (2026-08-27): layered hard/soft refusal + ModelRefusalError; CYBER_REFUSED terminal classifier path added; targeted refusal suites 37 pass |
| LIVE-VERIFY | Verify CLI thật T1-T4 qua gateway | parity | done (2026-08-24): T1 PASS(2 bug nền) · T2 PASS tool thật · T3 FAIL×2 prose-không-tool · docs/reports/live-cli-verify-2026-08-24.md |
| P1-4-IS-ERROR | Truyền is_error của tool_result vào `<WEBGPT_TOOL_RESULT>` — đóng lớp false-completion | parity | done (2026-08-25): parse đọc is_error → canonical role=tool → render `"is_error": true` chỉ khi lỗi (prompt gọn giữ nguyên khi false). Tests nguyên văn 15 passed |
| P1-3-BOUNDED-MULTI-TOOL | Cho N≤5 parallel tool calls/turn thay correction ngay (env WEBGPT_MAX_TOOL_CALLS_PER_TURN) | parity | done (2026-08-27 reconcile): bounded N tool calls env implemented, telemetry/goldens cover behavior; old duplicate TODO row superseded |
| OPENAI-USAGE-WIRE | Nối estimator usage vào nhánh OpenAI /v1/chat/completions (2 call site không truyền text, non-stream usage=None) — hoàn tất chuyện P0-1 cả 2 protocol | parity | done (2026-08-25): estimate_openai_usage() mới trong openai_types.py; 8 call sites + 2 helper wired cả stream lẫn non-stream trên CẢ HAI server; tests nguyên văn 36 passed + sanity 28/25 |
| P1-5-COUNT-TOKENS-ALIGN | Thống nhất count_tokens dùng cùng đường render→chars÷4 như StreamUsageEstimator (hiện 2 estimator tự mâu thuẫn) | parity | done (2026-08-26): `rendered_request_prompt` + `estimate_anthropic_input_tokens` trong protocol_adapters.py; 6 shape payload assert |count_tokens − StreamUsage| ≤1; align suite 10 pass + regression 91 pass + full suite 944 pass. docs/reports/p1-5-count-tokens-align-2026-08-26.md |
| P1-3-BOUNDED-MULTI-TOOL | Cho N≤5 parallel tool calls/turn thay correction ngay (env WEBGPT_MAX_TOOL_CALLS_PER_TURN, chỉ protocol non-cert) | parity | done (2026-08-27 reconcile): bounded N tool calls env implemented, telemetry/goldens cover behavior; old duplicate TODO row superseded |
| P1-2A-IMAGE-PLACEHOLDER | Chấm dứt drop ảnh âm thầm — placeholder [image omitted] khi CLI gửi ảnh | parity | done render-layer (2026-08-26): content_text() utils/promptcompat.py:89-90 thay silent-drop bằng `[image omitted: mime ~KB]`, kill-switch WEBGPT_IMAGE_PLACEHOLDER=0; tests/test_image_placeholder.py 7 pass (+84 kề bên). GAP còn lại → row ANTHROPIC-INGRESS-IMAGE. docs/reports/p1-2a-image-placeholder-2026-08-26.md |
| ANTHROPIC-INGRESS-IMAGE | /v1/messages thật strip ảnh ở parse_anthropic_request → _text_blocks (protocol_adapters.py) TRƯỚC khi tới render layer ⇒ placeholder chưa bắn trên đường Anthropic ingress | parity | done (2026-08-26): `_block_sequence_text()` protocol_adapters.py:53 giữ thứ tự block, image → marker qua content_text của P1-2A (dùng chung format + kill switch); áp user message + tool_result list; base64 vứt tại ingress không decode. +6 tests, targeted 21 passed, kề bên 109 passed. Ghi chú: /v1/responses vẫn drop input_image (thuộc CODEX-IMG-INPUT). docs/reports/anthropic-ingress-image-2026-08-26.md |
| LIVE-F4 | Sạch output stream: channel filter + anti-dedupe + strip prefix Thinking | transport | done (2026-08-24): 17 test mới, kill-switch riêng từng heuristic; suite 562 pass |
| LIVE-F1 | Stream đóng sạch sau message_stop | gateway | done (2026-08-24): cờ finished chặn ping sau terminator + WEBGPT_STREAM_DEADLINE_SECONDS → SSE error bounded; SDK hết treo |
| LIVE-F3b | Crash → 503 retryable | gateway | done (2026-08-24): decorator _translate_browser_crash session.py + fallback classify 2 server; session chết bị pool loại bỏ |

| CTFLIB | Bộ chọn bài CTF verify | QA | done (2026-08-24): 499 bài quét → 173 candidates (top-10 dễ nhất ≤40đ onboarding/crypto); scripts/pick_ctf_challenge.py + used-state chống lặp; JSON docs/reports/ctf-candidates-2026-08-24.json |
| T5-CTF | Giải bài CTF thật đầu tiên | parity | done (2026-08-24): **PASS attempt 1** — Invoice (onboarding forensics, 35đ), flag brunner{1_w0nt_p4y_th3m_4_d1me} xác minh độc lập trong VBA macro; ~115s, 9 tool_use khép kín, discover-first (ls đầu tiên), tự venv cài oletools khi thiếu; còn 172 bài |
| TOOL-PROTO/MULTI | json-fn protocol (WEBGPT_TOOL_PROTOCOL=xml|json-fn|both) — fence+bare+artifact "JSON\n" đều parse; correction prompt protocol-aware | parity | done (2026-08-24): 39 test, suite 722 pass; chưa flip env lên gateway (chờ R3 xong để không nhiễm phép đo) |
| DISCOVER-POLICY | WORKSPACE POLICY block (cả 2 protocol) + discover-first correction cho counter_question | parity | done (2026-08-24): 11 test; suite 733 pass |
| REV-SANDBOX | Reverse protocol sandbox gốc ChatGPT | parity | superseded (2026-08-26): soft protocol + correction stack đã thay vai trò sandbox reverse — row cũ không còn mục tiêu riêng |
| CF-RESILIENCE | CloakBrowser-first + challenge detect tập trung + re-mint recovery | hạ tầng | done (2026-08-24): fallback Chromium giờ REFUSE mặc định (opt-in WEBGPT_REQUIRE_CLOAKBROWSER=0); impersonate chrome146 + UA thật; 403-challenge → re-mint retry 1 lần; 15 test mới. 2 fail tạm thời ở test_claude_code_conformance do R6-FIX đang sửa server.py |
| CUSTOMGPT-PILOT | Trust instructions | parity | done (2026-08-24): **CONFIRMED 3/3** — model emit <cmd> chỉ nhờ instructions; đối chứng assistant thường 0 protocol; gizmo_id trong conversation_mode; dọn gizmo+profile sạch |
| GIZMO-WIRE | Wire gizmo routing vào gateway (12 điểm: SendRequest.gizmo_id, conversation_mode.kind=gizmo_test, position chọn gizmo...) khi cần persistence mạnh hơn soft handshake | parity | todo (chỉ nếu VERIFY-R7/PROMPT-LAB cho thấy soft chưa đủ) |
| PRACTICAL-BENCH | 3 bài thực tế CLI qua gateway (bugfix/feature/refactor) + auto-grading máy chấm | QA | V1 offline done; V2 offline DONE (2026-08-27): 5 scenarios materialized; dynamic discovery, per-task timeout, confinement/locked paths, red→green regression proof, TDD/git-history, mutation test-strength, held-out corpus, integration wall-budget, class-method mutation; `scripts/bench/selfcheck_practical_v2.py` pristine FAIL + solved PASS 5/5. Live gateway certification remains operational only. |
| PROMPT-LAB-1 | Đấu giá handshake | parity | done tạm (2026-08-24): GIỮ V1 baseline (variant duy nhất có tín hiệu hợp tác); phiên bị usage-cap 19:48 chặn nốt — cần 10 turn khi cap mở, harness sẵn /tmp/promptlab/lab.py; finding: soft không-khai-báo-tools giờ parse <cmd> → 502 Unknown tool nếu allowed_tools rỗng |
| PROMPT-LAB-2 | SOFT-COMPACT persistence policy | parity | done (2026-08-24): **A deflect 2/2 vs B đi tới flag 2/2** — text 77 từ ĐÃ MERGE vào _SOFT_HANDSHAKE_TEXT (runtime.py) + restart gateway; regression 49 pass |
| HANDSHAKE-V2 | SOFT_HANDSHAKE_TEXT = baseline + SOFT-COMPACT policy | parity | merged, chờ VERIFY-R7b dùng nó khi cap mở |
| T6-CTF | Bài thật thứ hai | parity | done (2026-08-24): **PASS attempt 1** — Decompile? (reversing 40đ), flag xác minh bằng cách chạy thật ./vault với 5 đáp án model (5× Correct); model chuyển sang static analysis (strings+objdump) khi interactive fail — không fake-success; 11 vòng cmd, 0 correction. **T5+T6 đều attempt 1 ⇒ tính lặp lại XÁC NHẬN** |
| T4-PERSIST | Persist TokenBundle vào profile dir | transport | done (2026-08-24): cache_dir optional, atomic 0600, fresh cache → 0 page.evaluate; 24 test-case pass. Lưu ý môi trường: 2 fail test_cf_resilience khi chạy bằng python hệ thống thiếu cloakbrowser — LUÔN dùng .venv/bin/python |
| HARD-CTF×5 | 5 bài khó (rev1000/crypto906/web957/misc857/pwn835) | parity | STOP theo yêu cầu owner (hard tốn thời gian); workspace partial còn ở /tmp/hard*, có thể quay lại sau |
| MED-CTF×5 | 5 bài medium 100đ đa category (crypto Slis · rev GoGoDecompile · forensics CompanyDiscount · web FairGambling · misc ActivatingNeurons) — trọng tài được quyền 2 subagent phụ, flock serialize, timeout 1200s | parity | done (2026-08-25): **4 PASS attempt-1 + 1 BLOCKED** — Slis/GoGoDecompile/CompanyDiscount/ActivatingNeurons flag verify độc lập (checksum, binary thật, stage3 byte-for-byte, Decimal precompute); FairGambling BLOCKED do DEFECT BỘ BÀI (flag redacted trong attachment, không remote) — model chứng minh exploit $1.8M local, báo cáo trung thực. Wave 46', 95 tool_use, 0 correction, quota vẫn mở. Report docs/reports/med-ctf-restart-2026-08-25.md; picker nên gắn NEEDS_REMOTE |
| MED-BATCH2-COMPLETION | Hoàn tất 3 bài PENDING sau crash: Free Play · Hidden Embeddings · Roadmap | parity | done (2026-08-25): **3/3 PASS attempt-1** (8'20" / 5'46" / 8'49") — verify độc lập: bit-run decode duy nhất toàn file · brute-force pure-Python chain [10,9,12,11] khớp 35 ký tự ASCII · backtrack checkpoint nginx CLEARED đủ 41 vị trí. **Batch 2 tổng kết 5/5 PASS**; quota VẪN MỞ, 0 correction, 0 retry. Report đã thay toàn bộ PENDING |
| VERIFY-FROMSCRATCH | Verify repo từ đầu ×5 trục read-only: test-suite phân tầng · parity-claims đối chiếu code · ops-docs tái lập · config-auth credential surface · architecture luồng+OOM | QA | done (2026-08-25): V1 697/697 xanh · V2 parity ~78% chức năng + P0-1 wire gap · V3 ROADMAP lệch 4 nhóm + 3 script không guard · V4 .env chmod + env scrub list · V5 architecture 5 điểm đứt + TOP-5 RAM → docs/reports/verify-fromscratch-2026-08-25/; findings đã chuyển thành các row hành động |
| PAYLOAD-BUDGET | Ngân sách prompt ≤10k chars trước khi send (trim tool-schema/tail) — root-cause chống rate-limit | parity | done (2026-08-25): flag WEBGPT_PROMPT_BUDGET_CHARS (default 0=tắt, khuyến nghị 10000) trong render_messages; thứ tự trim an toàn (nén tool JSON → drop history oldest → window sysdev head+tail), BẤT KHẢ XÂM: bootstrap/contract + user cuối + tool-call mới nhất. Trên 690 dump thật: vượt ngưỡng 40.9%→10.0%; idempotent; tests 20/20 + transpiler 28/28. Spec docs/reports/payload-budget-spec-2026-08-25.md. CHƯA bật trên unit — flip cùng lúc restart gateway (cần owner duyệt vì đổi hành vi payload) |
| BACKOFF-BREAKER | Backoff toàn cục + circuit breaker thay vì failover-resend tức thời khi đang throttle | reliability | done (2026-08-25): gpt/transport/breaker.py RateLimitBreaker (closed→open→half-open) + singleton toàn tiến trình — multi-account cùng bị chặn; gate trong acquire() TRƯỚC semaphore raise BackendCoolingDown; trip khi RL, probe khoẻ mở hoàn toàn + reset backoff. Cooldown 90s (WEBGPT_RATELIMIT_COOLDOWN_SECONDS, 0=tắt), ×2/probe-fail cap 600s. Tests 16 passed targeted + 16 kề bên |
| CORRECTION-TIGHTEN | Siết correction loop (cap 2 cho MALFORMED_TOOL, hint 1 lần, fail-fast) | parity | done (2026-08-26): protocol-shaped {MALFORMED_TOOL, INVALID_WRITE, MULTI_TOOL} sub-budget cứng min(env,2) → fail-fast `correction_budget_exhausted` + raise; anti-repeat sha256 base-prompt — trùng hệt lần trước → escalation hint đúng 1 lần, lặp nữa → "Correction loop not converging"; instrumentation: correction_count đếm event tool_correction (1/prompt gửi), turn_id từ result.turn_id mọi send + fallback failure events vào request_completed. Tests: 6 mới + kề bên 29+49+42 passed, evals total=19 pass=19. docs/reports/correction-tighten-2026-08-26.md |
| CODEX-PAYLOAD-CLI-SHAPE | Strip reasoning items + xoá max_output_tokens + headers session_id/version + instructions CLI-like trên nhánh codex/responses | parity | done (2026-08-25): reasoning strip helper :499 áp :492 · caps xoá :495 · _CODEX_VERSION 0.149.1 khớp CARGO_PKG_VERSION codex-rs · uuid ổn định per-transport · instructions <200 chars prose chuyển user input item. Tests nguyên văn 18 passed (+22 sentinel/cf) |
| CODEX-IMG-INPUT | Nhận image_url/base64 cả 2 protocol → input_image data-URL nhánh codex — giải quyết Read() screenshot/paste ảnh đang drop | parity | archived (owner decision 2026-08-26: no further Codex work); existing code/tests retained, no future live certification planned |
| USAGE-INTROSPECTION | Poll codex.usage() rate_limit.primary_window → breaker cooldown chủ động per-account TRƯỚC 429 | reliability | done phần code (2026-08-26): usage_poller.py (WEBGPT_USAGE_POLL_SECONDS default 0=tắt, bearer lazy từ codex_auth, parse defensive, 401 mute) + breaker.advise_pressure() ngưỡng 85% chỉ mở khi fully-closed, không compound; 38 passed (23 mới + 15 breaker). MỞ: URL /backend-api/wham/usage cần capture live chốt · reset_at bỏ qua · lifecycle wire point · env riêng cooldown advisory (docs/reports/usage-introspection-2026-08-26.md) |
| CF-CODEx-403-MONITOR | Monitor codex/responses bị Turnstile-gate theo thời gian (hiện 400-không-phải-403 nhưng bất biến chưa cam kết) | ops | archived (owner decision 2026-08-26: no further Codex work) |
| CODEX-AUTH-TOKEN-SOURCE | gpt/transport/codex_auth.py — PKCE bundle load/save + refresh rotation single-use serialize flock + invalid_grant → DEAD latch | parity | done (2026-08-26): flag WEBGPT_CODEX_AUTH_JSON vừa gate vừa path, default OFF zero I/O; 30/30 test fake-HTTP, full suite 976 pass; chưa live-probe contract OpenAI (lộ sai ở lần integration đầu). docs/reports/codex-auth-token-source-2026-08-26.md |
| VERIFY-R10 | Ladder sau reboot 2026-08-26 | parity | BLOCKED-upstream (2026-08-26): 3/3 turn đầu RateLimited từ ChatGPT Web (trace seq 9 `reason:"rate_limited"`, breaker trip 90→180→360s đúng thiết kế) — KHÔNG phải code gateway; T1-T3 không chạy được. Gate mở lại lúc ~07:55: coordinator probe T1 đơn lẻ PASS (first-token 3.06s) ⇒ baseline khoẻ đủ để bắn MED-BATCH4. Ladder đầy đủ sẽ gộp vào verify sau batch. docs/reports/verify-r10-2026-08-26.md |
| CODEX12-FIX | Vá finding codex review #12 (~/Downloads/webgpt/codex-reviews/codex12-yesterday-fixes-2026-08-26.md) theo 3 cụm file: A toolcall.py (#2 High fence-injection scan masked-text + #5 Low span-trim raw_calls rỗng) · B runtime.py (#1 High fresh-guard vô hiệu breaker no-op sau tool-use + #6 Low budget check trước handshake) · C transport (#3 Med fconv 401/403 không invalidate Bearer + #4 Med PoW 500k vòng block event loop → to_thread) | QA | done (2026-08-26): all six findings A/B/C closed; row was stale |
| CODEX-AUTH-INTEGRATION | Wire codex_auth vào nhánh codex/responses (bearer khi WEBGPT_CODEX_SSE=1 + WEBGPT_CODEX_AUTH_JSON; 401 rotate-once retry; DEAD propagate) | parity | done (2026-08-26): curl_transport.py :203/:324/:1139 lazy-import injection; tests/test_codex_auth_integration.py 7 passed độc lập (92 targeted cluster); ruff+mypy sạch. docs/reports/codex-auth-integration-2026-08-26.md |

## Rows từ PARITY-DELTA-AUDIT 2026-08-26 (docs/reports/parity-delta-audit-2026-08-26.md — parity chức năng 78→81%, weighted ~85-88%)
| STREAM-CORRECT-DEDUP | Chặn text trùng khi correction giữa stream: ranh giới attempt trong session.events / chỉ bật live-delta sau classification đầu; remainder reconciliation per-attempt; test FALSE_COMPLETION-mid-stream (runtime.py:1792-1794,2156 + gateway/server.py:1640-1710) | parity | done (2026-08-26): stream correction dedup implementation/tests present; stale TODO reconciled 2026-08-27 |
| LATE-FAIL-SURFACE | started_content=False → phát `event: error` chuẩn thay vì close sạch; đã stream thì giữ close sạch + metric `late_failure_masked` | parity | done (2026-08-26): cả 2 server (gateway + api); pre-content lỗi → 1 frame `event: error` envelope Anthropic (type ngoài contract coerce về api_error), không terminator/ping theo sau; đã-stream → giữ nguyên close sạch R4 + counter `server.late_failure_masked` + log `late_failure_masked`; cập nhật 4 test khóa R4, thêm 3 test mới; docs/reports/late-fail-surface-2026-08-26.md |
| ANTHROPIC-FIELDS-EXPLICIT | parse_anthropic_request: 400 rõ cho stop_sequences non-empty/thinking enabled; metadata accept-ignore có log; placeholder block document | parity | done (2026-08-27 reconcile): explicit Anthropic field handling present and covered in current suite |
| STOP-REASON-REFUSAL | Model-refusal → 200 + stop_reason:"refusal" thay vì 502; hạ tầng lỗi giữ nguyên status | parity | done (2026-08-26): ModelRefusalError subclass, convert có điều kiện :2117/:2141/:2096 (audit ghi breaker-trip chưa chuẩn — :2031 trip mọi reason kể cả FALSE_COMPLETION nên giữ 502); Anthropic non-stream 200 envelope refusal + stream đóng completed-turn; OpenAI giữ 502. 11 test mới, 9 suite 142 passed |
| HEADER-PARITY | request-id echo + anthropic-ratelimit-* advisory tĩnh từ breaker state | parity | done (2026-08-26): middleware request-id + anthropic-ratelimit-* từ breaker snapshot cả 2 server |
| PING-WIRE | ": ping" → _sse_event("ping") đúng spec | parity | done (2026-08-26): _sse_event ping cả 2 server, chặn-ping-sau-terminator giữ nguyên |
| JSON-DELTA-CHUNK | Chunk partial_json ~512 ký tự mô phỏng streaming progress | parity | done (2026-08-26): chunk 512 ký tự mirror cả 2 server, concat == cũ |
| OVERLOADED-529 | Map RateLimited-có-flag-overload → HTTP 529 | parity | done (2026-08-26): _is_overloaded_rate_limit → 529 overloaded_error, RateLimited thường giữ 429 |
| IMAGE-UPLOAD-WEB | Upload ảnh thật attachment endpoint ChatGPT Web, fallback placeholder | parity | IMPL DONE (2026-08-26): file_upload.py pipeline 3 bước + cache sha256→file_id; curl_transport flag WEBGPT_IMAGE_UPLOAD_WEB (OFF, fail-open placeholder); multimodal_text+image_asset_pointer cap 10; 22 passed + regression 122; ruff sạch. CÒN: live-verify 1 PNG probe trước khi ON (research DONE (2026-08-26, docs/reports/image-upload-research-2026-08-26.md — 3 nguồn độc lập khớp byte-level, mạnh nhất gptweb2api): files→upload_url(Azure PUT)→uploaded→multimodal_text image_asset_pointer; không cần sentinel; TokenBundle sẵn đủ. Effort hạ L→S-M; flag WEBGPT_IMAGE_UPLOAD_WEB default OFF fail-open placeholder. IMPL in-progress |

| MED-BATCH4 | Refire 5 bài medium khi gateway khoẻ trở lại | parity | DONE-AS-BLOCKED ×2 (2026-08-26): refire ~11:2x vẫn 0/5 — upstream `commit_unknown` nuốt conversation âm thầm (quota token-weighted từ 07:55 chưa nhả; giải thích T1 nhỏ PASS còn turn lớn chết); 9 POST cả ngày, tool_use=0, used-list giữ nguyên 12 entry. Prep refire nguyên trạng trong /tmp/medctf4/ref/. CẦN OWNER: login web kiểm tra tài khoản thủ công. Phát hiện phụ: crash-loop registry đã xử lý (xem FAILURES 2026-08-26 accounts.json) |
| QUOTA-PREFLIGHT | scripts/preflight_quota.py đọc wham/usage trước mỗi batch; primary≥70%/secondary≥50% → exit 2 dời batch | ops | done code (2026-08-26): exit 0/2/3 (0=ok · 2=defer kèm JSON · 3=unknown không đoán path); 75 passed poller+preflight; ruff sạch. Endpoint wham/usage CHƯA live-verify — 404/401 thật → báo đổi path có chủ đích. docs/reports/quota-preflight-2026-08-26.md |
| LIMIT-SIGNATURE-TAXONOMY | Phân lớp 429-vs-challenge trước khi nuôi breaker; chỉ 429 thuần tính trip | transport | done (2026-08-26): LimitSignal + classify_limit_signal trong challenge.py (CHALLENGE > JSON-signature > bare-429 > UNDETERMINED); gate peek-body trong _raise_for_status; breaker chỉ ăn RateLimited; conftest scrub +2 env accounts; 27+12+14+22 passed |
| RESET-AWARE-COOLDOWN | Dùng reset_at/window trong usage_poller+breaker: cooldown=min(cd, reset_at−now+60s); window <90s → skip advisory open | transport | done (2026-08-26): kw-only backward-compat; 9 passed breaker regression |
| ACCOUNTS-REGISTRY-RESILIENCE | Tìm thủ phạm xoá accounts.json + backup-on-write registry + startup warn nếu thiếu | reliability | done (2026-08-27 reconcile): thủ phạm KHÔNG trong repo; phòng tuyến .bak.1..3 rotation atomic 0600 + startup warn đã có; tests/conftest.py scrub `WEBGPT_ACCOUNTS_FILE`, `WEBGPT_PROFILES_ROOT`, `PROFILE_DIR`, nên TODO isolation cũ đã đóng. docs/reports/accounts-registry-resilience-2026-08-26.md |

| CODEX13-FIX | Vá finding codex #13 (rotate-once không force-refresh · conduit nuốt 401/403 · masker backtick-lẻ · correction_count khai thừa) | QA | A ✅ done (2026-08-26): force_refresh bypass disk-fresh + untrusted latch sau 401×2; conduit/fconv 401→_invalidate_access_credentials; 9 test RED→GREEN, 63+137 passed (docs/reports/codex13fix-a-transport-2026-08-26.md) · B ✅ done — #3 ĐÚNG fix `_soft_tag_regions` pre-scan shield (body không nuốt close-tag, fence/inline echo vẫn chặn) · #4 ĐÚNG trừ counter nhánh abort; 447 green (docs/reports/codex13fix-b-parser-2026-08-26.md). CỘNG ĐỒNG: codex #13 đóng hồ sơ 4/4 |

| GOLDEN-EXPAND | Khóa hành vi mới vào eval goldens | QA | done (2026-08-26): +5 goldens 20-24 (noop-transcript FALSE_COMPLETION · real-work accepted · escalation-once telemetry=2 · late-fail pre-content raise_on_send · placeholder-excision); golden 14 correction_count 3→2 khớp semantic mới; EVALS total=24 pass=24. Scratch còn ở ~/Downloads/gpt-goldenexpand-scratch (owner tự xóa) |
| FCONV-NOTOKEN-REPLAY | Thêm X-Conduit-Token: no-token vào _prepare_fconv_turn (curl_transport.py:452, flag WEBGPT_FCONV_PREPARE có sẵn) + script replay thang ≤4 request (requirements→PoW→prepare→conversation) | parity | done — **VERDICT ALIVE** (2026-08-26 ~17:1x): 4/4 bước 200; capture SSE thật `{"type":"resume_conversation_token","kind":"topic",token:eyJ…}`; persona chatgpt-paid xác nhận auth. Script fix2 aiter_content. NEXT: FCONV-E2E-WIRE trên instance test (IMPL DONE (2026-08-26): header no-token + scripts/cert/fconv_replay.py (dry-run default, --live từ chối nếu flag OFF, dừng-sớm từng bước in evidence); 24/24 test. LIVE REPLAY chờ R11 xong rồi coordinator bắn: `WEBGPT_FCONV_PREPARE=1 .venv/bin/python scripts/cert/fconv_replay.py --live --profile <dir>` Tiêu chí chốt: prepare 200+conduit mà conversation vẫn 403 ⇒ đóng nhánh vĩnh viễn dồn CODEX-SSE OAuth. Nguồn: docs/reports/sse-resume-research-2026-08-26.md (probe 25/8 vô hiệu vì thiếu body+token!) |
| FCONV-RESUME-HANDOFF | Parser giữ resume_conversation_token + follow POST /f/conversation/resume offsets 0→2 cap 64 | parity | done (2026-08-26): parser capture kwarg + _follow_fconv_resume_segment (advance-when-404 · X-Conduit-Token captured · cap 64 · loop-guard same-token · never-raise); TurnResult.metadata field; WEBGPT_FCONV_RESUME default OFF byte-for-byte cũ; 12 test mới + 189 regression xanh |
| POOL-POLLER-PERACCT | UsagePoller per-account + least-pressure selection + cross-brake K/N | multi-account | done (2026-08-26): PoolPressureBoard + create_account_pollers (credential ~/.config/webgpt/codex/<name>.auth.json) · multi_account pressures/cross_brake/selection kwargs · CrossBrake K@seconds sliding window; env WEBGPT_POOL_SELECTION/CROSS_BRAKE/AUTH_DIR; 97+210 passed; lifespan wire → agent riêng đang chạy. docs/reports/pool-poller-peracct-2026-08-26.md. (Row trước: POOL-PER-ACCT-BREAKER done code (2026-08-26): multi_account.py breakers kwarg + skip-open (half_open vẫn probe) + retry-next ≤N−1; hybrid.py optional inject; 22 test mới, full suite 1243 passed; default global byte-for-byte cũ. docs/reports/pool-per-acct-breaker-2026-08-26.md. Bước M kế: poller per-account + least-pressure (TODO — chờ CODEX14-FIX nhả 2 file server; bước M kế: UsagePoller instance/account + least-pressure selection (docs/reports/multi-account-pool-research-2026-08-26.md; rủi ro ToS ban-wave — quyết định pool bao nhiêu account thuộc owner) |
| MODEL-ROUTING | Route model CLI→ChatGPT qua root field "model" slug trong POST f/conversation (+thinking_effort) — hạ tầng 80% sẵn (payload builder :1082 + parser capture resolved_model_slug :2087) | parity | phase-1+2 done (2026-08-26): P1 alias-map (_MODEL_ALIAS_ENV/:219/:274/:292) · P2 downgrade telemetry (resolved_model/downgraded/model_downgrade_count TurnResult) + retry-once marker `[webgpt:model-fallback]`; env WEBGPT_MODEL_FALLBACK=warn|retry-once; 16 test routing + 102 suite + full 1293 pass. docs/reports/model-routing-phase{1,2}-2026-08-26.md (2026-08-26): env alias-map opt-in (ModelRegistry đang ignore claude-* có chủ đích) + effort-first policy + verify-slug-each-turn chống SILENT DOWNGRADE server-side (rủi ro số 1 — OmniRoute #4665, Plus 8/2026 serve nhầm mini 100%). DEFER chờ FCONV-REPLAY-IMPL nhả curl_transport.py. Nguồn: docs/reports/model-routing-research-2026-08-26.md |
| PICKER-V3 | Flag-in-docker detection + nested archive REDACTED scan (zip-bomb budget) + --unmark flow | QA | done (2026-08-26): 23 passed (+12 mới); dry-run thật 499 dirs → needs_remote 19→29 (+10 reclassified, đủ 4 blind spot PTIT), 0 mất bài, sidecar không ghi. docs/reports/picker-v3-2026-08-26.md |
| GOLDEN-REFUSAL-POOL | Goldens khóa refusal mapping + pool breaker | QA | CLOSED-as-covered (2026-08-26): hành vi đã khóa bằng unit tests (test_stop_reason_refusal.py 11 test · test_pool_breaker.py 22 test); agent chết stall-upstream không artifact — không resume tránh vòng lặp; evals giữ 24/24 |

| OAI-DID-FALLBACK | TokenManager đọc cookie `oai-did` (tên thật ChatGPT đặt) + mint/persist UUID4 fallback khi rỗng — fix AuthRequired đường fconv | transport | done (2026-08-26): ưu tiên oai-did → oai-device-id → oai_device_id → localStorage; mint+persist qua webgpt-token-cache.json field oai_device_id; 18+28 passed, ruff sạch. **E2E RERUN PASS** (2026-08-26 ~18:1x): T1 lần đầu trúng — HTTP 200, 5 deltas, output đúng, first-token 11s; evidence fconv path (FCONV_PREPARE env + submit trace conversation_id thật + oai-did trong token cache). T2 PASS + T3 PASS lần đầu (docs/reports/fconv-t23-wire-2026-08-26.md) — SẴN SÀNG FLIP: root cause từ E2E wire test (docs/reports/fconv-e2e-wire-2026-08-26.md — E2E FAIL đúng 1 điểm này, protocol fconv ALIVE xác nhận lần 2); xong → re-run E2E lấy evidence rồi quyết flip |
| IMG-MARKER-ESCAPE-FIX | Marker <WEBGPT_IMAGE_DATA> bị JSON-encode vỡ (<→<) ở render layer trước transport — upload recipe không bao giờ trigger trên ingress thật | parity | done local (2026-08-27): regex accepts render_messages escaped markers + dedicated render→collector PNG regression; image tests 30 pass. LIVE PNG recert remains under IMAGE-UPLOAD-WEB; flag stays OFF |

| PICKER-RISK-SCORE | Risk tier category+artifact+statement, --max-risk, risk-first sorting | QA | done (2026-08-27): implemented in scripts/pick_ctf_challenge.py; 25 picker tests pass |
| SOLVER-REFRAME-TEMPLATE | Shared truthful authorized/local-first CTF prompt framing | orchestrator | done (2026-08-27): scripts/legacy/ctf_prompting.py wired into both CTF solver entrypoints; targeted suite green |
| GATEWAY-CYBER-REFUSAL | Detect web cybersecurity-classifier refusal as terminal CYBER_REFUSED | parity | done (2026-08-27): no correction-loop burn in poisoned conversation; ModelRefusalError + trace event; refusal suites 37 pass |
| BENCH-V2-ENGINE | Dynamic discovery, per-task timeout, confinement/locked paths, stronger grading primitives | QA | done offline (2026-08-27): model-regression red→green, ordered/TDD git-history proof, model-written test-strength mutation, held-out corpus, integration wall-budget, class-method mutation; 5 V2 scenarios materialized; `scripts/bench/selfcheck_practical_v2.py` proves pristine FAIL + solved PASS 5/5. Live gateway certification remains operational work, not engine implementation. |
| QUALITY-REPO | Ruff/mypy repo-wide cleanup | QA | done local (2026-08-27): `ruff check .`=0, `mypy .`=0, full pytest 1325 pass after cleanup; intentional benchmark fixtures are scoped by config rather than auto-mutated. Review gate now enforces repo-wide Ruff + configured mypy. |
| SOFT-SURFACE-HANDSHAKE | Negotiate executable soft emit format from actual tool surface | parity | done local (2026-08-27): shell-capable surface teaches `<cmd>`; function-only surface teaches `<json>`; correction + anti-repeat escalation + prompt-budget reserve use same surface; dedicated regressions added. |

| SOAK-TRACE-CORRECTIONS | Tự động tổng hợp correction telemetry từ gateway trace vào soak report | QA | done local (2026-08-27): `--trace-file` snapshots sequence trước run, chỉ đọc `request_completed` mới; report có corrected requests / total corrections / max per request; malformed trace fail-open có đếm; 3 regression mới, `tests/test_soak_runner.py` 34 pass; full pytest 1328 pass, Ruff/mypy/evals/Bench V2 xanh. |
