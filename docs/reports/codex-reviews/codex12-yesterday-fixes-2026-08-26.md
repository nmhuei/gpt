1. **High — the new freshness guard disables the no-op FALSE_COMPLETION breaker it is meant to protect.** After `<cmd>true</cmd>` is accepted, `_fresh_tool_conversation()` returns false because the transcript now has an assistant tool call/tool result. Subsequent prose false completions therefore never reach `reason == "FALSE_COMPLETION"`, making the no-op skip at runtime.py:1930 effectively unreachable for the documented metronome. It also leaves later independent tool tasks unguarded. [runtime.py](/home/light/GitHub/gpt/gpt/gateway/runtime.py:314), [runtime.py](/home/light/GitHub/gpt/gpt/gateway/runtime.py:769), [runtime.py](/home/light/GitHub/gpt/gpt/gateway/runtime.py:1930)

2. **High — soft parsing executes `<cmd>` tags inside Markdown/code fences.** `_extract_soft_candidates()` scans the original text rather than the markdown-masked text, so a model echoing quoted/untrusted text such as ``` `<cmd>…</cmd>` ``` produces an executable Bash call. This is a tool-execution injection risk, particularly with the new prose tolerance. [toolcall.py](/home/light/GitHub/gpt/gpt/utils/toolcall.py:554), [toolcall.py](/home/light/GitHub/gpt/gpt/utils/toolcall.py:582), [toolcall.py](/home/light/GitHub/gpt/gpt/utils/toolcall.py:1169)

3. **Medium — f/conversation authentication failures retain a rejected access-token bundle.** The new fconv path authenticates prepare and SSE requests with the Bearer token, but 401/403 only invalidates sentinel state unless `codex=True`; prepare-stage 401/403 is raised without invalidating either cache. Subsequent requests can reuse the known-bad cached Bearer token until refresh expiry. [curl_transport.py](/home/light/GitHub/gpt/gpt/transport/curl_transport.py:381), [curl_transport.py](/home/light/GitHub/gpt/gpt/transport/curl_transport.py:1077)

4. **Medium — server-controlled PoW blocks the async event loop.** `solve_sentinel_pow()` performs up to 500,000 JSON/hash iterations synchronously, and `_prepare_fconv_turn()` calls it directly from `async def`. A difficult challenge stalls all work sharing that loop and prevents timeout/cancellation processing. [token_manager.py](/home/light/GitHub/gpt/gpt/transport/token_manager.py:292), [curl_transport.py](/home/light/GitHub/gpt/gpt/transport/curl_transport.py:401)

5. **Low — placeholder span trimming does not occur when every soft tag is filtered.** The placeholder branch records the tag span, but `_finalize_parse()` returns the original `text` unchanged when `raw_calls` is empty. Thus the quoted `<cmd>...</cmd>` remains visible despite the comment promising it is excised. [toolcall.py](/home/light/GitHub/gpt/gpt/utils/toolcall.py:586), [toolcall.py](/home/light/GitHub/gpt/gpt/utils/toolcall.py:1127)

6. **Low — prompt-size enforcement excludes the handshake/framing text.** The maximum prompt check runs before `_with_soft_handshake()`, so soft prompts near the configured limit can exceed it by the appended handshake and framing payload. [runtime.py](/home/light/GitHub/gpt/gpt/gateway/runtime.py:1703), [runtime.py](/home/light/GitHub/gpt/gpt/gateway/runtime.py:1707)

No direct credential values are logged in the reviewed transport changes. `pytest` could not run because this read-only environment has no writable temporary directory; AST parsing and `git diff --check` passed.