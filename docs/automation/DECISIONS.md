# DECISIONS — append-only

- [2026-08-24] Bỏ qua các issue bảo mật (gateway no-auth, RCE-by-design orchestrator, plaintext cred) theo quyết định owner; tập trung chức năng/ổn định/tốc độ.
- [2026-08-24] Protocol-first: DOM chỉ là fallback cuối; browser chỉ dùng để mint token; mọi turn đi HTTP/WS.
- [2026-08-24] Browser luôn headless chạy ngầm, cấm fallback headful.
- [2026-08-24] Config precedence: environ > .env > default; launcher `gpt` là symlink vào scripts/webgpt-claude.sh, pyproject không khai báo entrypoint `gpt`.
- [2026-08-24] Sentinel finalize body chuẩn: `{"prepare_token": …}` (probe 200); `{"p": …}` bị 500.
- [2026-08-24] Vòng lặp 24/7 áp memory-bank pattern: 4 file trạng thái trong docs/automation/, retry-cap 2 lần, phân loại transient/permanent, task-blocked-2-lần → dừng dispatch.
- [2026-08-24] Verify chức năng thật = giải bài CTF từ ~/Workspace/CTF: loại bài đã giải (có flag.txt), chỉ chọn bài đề rõ ràng + local giải được hoặc remote còn sống; mỗi verify dùng bài chưa từng dùng (state used_at).
- [2026-08-24] Tool protocol hướng tới JSON function-calling (model emit 3/3 nhất quán) thay XML tự chế (0/2); delimiter PHẢI là sentinel tag thuần văn bản — markdown fence chết ở inner_text() scrape (ui.py:815). Nguồn: docs/reports/meta-gpt-tool-format-2026-08-24.md
- [2026-08-24] Agent behavior policy: DISCOVER-FIRST — model phải tự pwd/ls/find trước khi hỏi controller; workflow DISCOVER→INSPECT→ANALYZE→ACT→VERIFY; câu hỏi ngược về thứ tự tìm được = counter_question → correction kèm dòng discover-first. (Phân tích owner chuyển tiếp)
- [2026-08-24] STEALTH PROTOCOL là hướng chính cho parity tool-call: KHÔNG khai báo tools qua API, KHÔNG chèn banner protocol; một câu giao kèo ngắn giọng tự nhiên ở turn đầu + parser tag thuần văn bản (<cmd>/<json>). Bằng chứng: soft-framing-probe 4/4 vs tools-declared 0/4.
- [2026-08-24] MỐC: tool-use đơn bước qua ChatGPT Web ĐẠT bằng STEALTH protocol (VERIFY-R5, T2 pass lần 1 không correction). T3 còn chờ fix delta-hop + handshake-per-thread.
- [2026-08-24] Chiến lược Cloudflare: CloakBrowser-first mọi đường + nhất quán fingerprint + phát hiện sớm + re-mint clearance qua browser thật khi bị chặn. KHÔNG xây CAPTCHA solver — prevention only.
- [2026-08-24] Ngôn ngữ phản hồi gateway→model: giữ soft <cmd> + WEBGPT_TOOL_RESULT text thường (pseudo-aggregate_result không mua thêm gì — model tin controller output bất kể format). Tool schema phải ổn định suốt session (409 nếu đổi).
- [2026-08-24] CUSTOMGPT TRUST CONFIRMED (pilot 3/3): instructions gizmo = trusted context cho tool protocol; gizmo_id nằm TRONG conversation_mode {"kind":"gizmo_test","gizmo_id":...}; draft gizmo chỉ chat qua /gpts/editor preview; REST f/conversation vẫn 403 dù full sentinel — UI driver là đường end-to-end duy nhất đã chứng minh. Cảnh báo: 1 account KHÔNG mở 2 fingerprint browser song song (session invalidate). Nguồn: docs/reports/custom-gpt-pilot-2026-08-24.md
- [2026-08-24] 🏁 MỐC PARITY: 'Claude Code CLI tự chủ đa bước qua ChatGPT Web' ĐẠT (VERIFY-R7d, T3 pass turn đầu: fizzbuzz.py+output.txt đúng, 12 tool_use khép kín, 0 correction). Công thức thắng: stealth protocol + SOFT-COMPACT handshake + task-context correction + handshake-per-thread + môi trường không co-tenant.
- [2026-08-24] 🏁 THANG VERIFY ĐẦY ĐỦ: T1 streaming · T2 tool đơn bước · T3 đa bước tự chủ · T5 giải CTF thật (Invoice, brunner{...}) — TẤT CẢ ĐẠT qua stealth protocol. Tool chính thức làm được mục đích tồn tại: chạy Claude Code qua ChatGPT Web giải CTF.
- [2026-08-25] OWNER CHỐT TRANSPORT-MODE: flip systemd unit từ --transport browser sang --transport hybrid (browser chỉ mint token, generation đi HTTP protocol — đúng nguyên tắc protocol-over-DOM). Điều kiện thực thi: chờ wave hiện tại merge xong + full pytest xanh rồi mới restart; sau flip phải chạy lại thang verify T1→T3, đỏ 1 lần retry → rollback unit về browser. KHÔNG bật kèm PAYLOAD-BUDGET flag khi chưa có duyệt riêng.
- [2026-08-26] CHÍNH SÁCH QUOTA VẬN HÀNH (từ research pattern hạn mức): duy trì ≤8–10 msg/ngày/account (burst ≤25 trong cửa sổ 3h); PREFLIGHT đọc wham/usage BẮT BUỘC trước mọi batch (scripts/preflight_quota.py — exit 0 = mở, exit 2 ≥70% primary hoặc ≥50% secondary = DỜI batch, exit 3 = unknown do coordinator tự quyết); phân loại block: 429 thuần → cooldown phút · DOM dialog "usage limit" → coi như 429 · primary ≥95% → park đến reset_at+15' · secondary ≥95% → park tới ngày reset tuần · 403/503 HTML challenge → KHÔNG phải quota, đường challenge.py. Nguồn: docs/reports/quota-pattern-research-2026-08-26.md
- [2026-08-26] CODEX-AUTH PATH: access token web-session CHÍNH THỨC bị codex backend từ chối làm credential (liveprobe CODEX-SSE 2×POST → 401) ⇒ đường sống duy nhất là mint bundle OAuth PKCE NGOÀI CLI qua `scripts/codex_oauth_login.py` ghi `~/.codex/auth.json` (mode 0600, state verify, không bind port), gateway đọc qua `WEBGPT_CODEX_AUTH_JSON`. Refresh chain chết (reuse/8 ngày idle) → chạy lại helper với `--force`. Nguồn: docs/reports/codex-oauth-login-helper-2026-08-26.md
- [2026-08-26] IMAGE-UPLOAD-WEB: chấp nhận triển khai recipe upload ảnh ChatGPT Web 3 bước Azure blob (POST /backend-api/files → PUT upload_url raw bytes + x-ms-blob-type → POST /files/{id}/uploaded, poll nếu processing) với flag `WEBGPT_IMAGE_UPLOAD_WEB` mặc định OFF và fail-open (mọi lỗi → giữ placeholder `[image omitted]` như cũ). Chưa live-verify — chỉ bật ON sau 1 PNG probe thật. Nguồn: docs/reports/image-upload-web-impl-2026-08-26.md
- [2026-08-26] ACCOUNTS REGISTRY RESILIENCE (phản ứng sự cố accounts.json bị xoá sáng nay, crash-loop 94 respawn): backup-on-write — mỗi lần ghi đè registry snapshot trạng thái cũ thành `.bak.1..3` xoay vòng ngay cạnh file (atomic replace, mode 0600, lỗi backup chỉ WARNING) + startup warn trong AccountStore.__init__ khi registry KHÔNG tồn tại nhưng profiles_root còn entry con (log lộ liễu ngay từ đầu thay vì respawn câm). Nguồn: docs/reports/accounts-registry-resilience-2026-08-26.md

## [2026-08-26] FLIP HYBRID PRODUCTION — browser → hybrid/fconv
**Quyết định:** unit webgpt-gateway chuyển `--transport browser` → `--transport hybrid` + `WEBGPT_FCONV_PREPARE=1`. Backup unit browser: `webgpt-gateway.service.bak-browser-20260826`.
**Cơ sở:** T1/T2/T3 PASS trên đường fconv thuần (test instance :18001 rồi production :18000 sau flip); replay 4/4 bước 200 với SSE thật; protocol fconn được minh oan — probe 25/8 kết tội sai do thiếu body + X-Conduit-Token; fix oai-did cookie fallback.
**Rollback:** đỏ 1 retry → khôi phục unit backup → daemon-reload → start (điều kiện DECISIONS 2026-08-25).
**Phát hiện kèm theo:** classifier ChatGPT chặn stochastic task màu reverse-engineering (khác auth/quota); profile_dir trong accounts.json là absolute path — move data phải patch registry.

- [2026-08-31] UNIFIED DIRECT-AGENT UX supersedes the 2026-08-24 launcher decision: `gpt` is now the primary console entrypoint (`gpt.cli.main:main`) and talks directly to the local gateway with its own Bash/ApplyPatch tool loop, session store and controller verification gate. Claude Code is compatibility-only via `gpt-claude` / `gpt compat claude`. Stable user config precedence is CLI > env > project `webgpt.toml` > user XDG config > defaults. Script surface is reorganized: nine root operational entrypoints; specialized workloads live under `scripts/bench/`, `scripts/cert/`, `scripts/auth/`, `scripts/legacy/`. Construction-time gateway knobs use typed `GatewayTuning`; runtime kill-switch/debug flags remain hot env reads. Typed feature defaults mirror transport defaults: image upload OFF, fconv resume OFF, usage polling OFF until explicitly enabled.
