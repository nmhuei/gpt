# E2E-BENCH Report — 2026-08-25

Trọng tài: agent LIVE duy nhất. Claude Code CLI → gateway `http://127.0.0.1:18000` (backend ChatGPT Web stealth protocol). Workspace: `/tmp/e2ebench-ws` (đã xoá sạch sau chấm). Harness + grade scripts: `/home/light/Downloads/e2ebench/{run_bench.py,grade.py}`; trace nguyên văn: `/home/light/Downloads/e2ebench/{stream.jsonl,run.log,result.json,grade.json}`.

## Verdict: PASS

## Phase table

| Phase | Nội dung | Kết quả | Số liệu nguyên văn |
|---|---|---|---|
| 0 | Health probe gateway | OK | `{"ok":true,"status":"ok","browser":"ready",...,"active_sessions":64}` — 1 request |
| 1 | Live build (1 claude `-p` session, prompt 1,314 chars) | Exit 0 | turns=50, tool_use=49, tool_errors=26, pytest_invocations=21, wall=436290 ms (7m16s), num_turns=50, duration_ms=436290 |
| 2a | Inventory máy đếm | PASS | pyproject.toml ✓, README.md ✓, tests/ ✓, lib module `textkit/core.py` với `slugify`+`extract_emails` ✓ |
| 2b | AST audit test quality | PASS | 6 test functions, 6 asserts trong test body, trivially_passing_asserts=0 |
| 2c | Pytest độc lập (venv riêng của workspace, pip install pytest từ PyPI) | PASS | `pip_rc=0`; `.venv-grade/bin/python -m pytest -q` → rc=0, `6 passed in 0.01s`; plain `python -m pytest` rc=0 |
| 2d | Mutation check (bản COPY: slugify override → `'MUTATED'`) | BINDS | pytest rc=1 (fail khi hàm bị phá) → test thật sự gắn hành vi, không trivially-pass |
| 3 | Dọn dẹp OOM discipline | DONE | `/tmp/e2ebench-ws`, `/tmp/e2ebench-mutcopy` đã xoá; không còn `/tmp/e2ebench*` |

## Quan sát trace (correction episodes)

26/49 tool calls trả lỗi; model tự sửa ≥3 vòng:

1. File bị ghi sai tên `textkit/init.py` thay vì `__init__.py` (heredoc mất underscore) → sửa bằng `printf` rồi `python3 -c pathlib.write_text`.
2. Typo trong test: `sluggify`, `test_sluggify_punction(i)` → sed rename về đúng.
3. Expectation sai `slugify(" A---BA! ") == "a-b"` → tự sửa thành `"a-ba"` (khớp implementation collapse-hyphen).

Không thấy 429/rate-limit nào trong run.log.

## Nhận xét chất lượng code model tạo ra

`slugify` dùng chuẩn nghề: `unicodedata.normalize("NFKD")` → ASCII-fold → `re.sub(r"[^a-zA-Z0-9]+","-")` → `strip("-")`. Điểm trừ nhỏ: regex email có dot chưa escape ở phần TLD (`[A-Za-z0-9.-]+.[A-Za-z]{2,}`) và để lại rác file `textkit/init.py` chưa dọn.

## Ràng buộc tuân thủ

Không restart gateway; không chạy pytest suite chính repo; không commit; workspace + copy /tmp đã xoá; artifacts lớn lưu `~/Downloads/e2ebench/`.
