# VERIFY-R11 — Full Ladder chứng nhận stack mới sau merge (2026-08-26)

**Verdict tổng: BLOCKED-upstream-rate-limit — KHÔNG phải regression gateway. Cả hai lượt T1 chạm
backend thật đều bị upstream rate-limit ngay (trip #1 penalty 90s → đợi đủ 1 chu kỳ cooldown →
trip #2 penalty 180s). Theo luật verify: hết ngân sách (initial + 1 retry/sau 1 chu kỳ cooldown),
chốt BLOCKED, không đốt thêm turn. Các cơ chế mới của stack quan sát được trong cửa sổ blocked
đều vận hành đúng thiết kế (failover map vào SSE error event, breaker fast-fail có countdown,
taxonomy `reason=rate_limited`, `correction_count=0` toàn phiên).**

> Phân biệt với R10 (sáng cùng ngày): cùng pattern upstream rate-limit theo tài khoản, nhưng R10
> bị ngay sau reboot còn R11 bị giữa phiên (T1 của coordinator PASS lúc 14:26 ⇒ hạn mức còn sống
> lúc đó, chết trước 15:00). Không thấy co-tenant nào tranh gateway trong cửa sổ verify.

## Điều kiện & môi trường

- Gateway: systemd `webgpt-gateway.service`, PID **4105024**, start **Wed Aug 26 14:26:38 (+07)**,
  port 18000, `--transport browser --account personal --max-workers 8 --warm-workers 4`,
  `WEBGPT_TOOL_PROTOCOL=soft`. Uvicorn KHÔNG `--reload`. Không restart, không sửa code, không flip
  env trong suốt R11 (bởi verify agent).
- Thang: `scripts/verify_hybrid_flip.py --level all --timeout 120`. Đã đọc source trước khi chạy:
  main-guard đúng (`if __name__ == "__main__": sys.exit(main())` dòng 581), chạy bằng
  `.venv/bin/python`.
- Trace: `/home/light/Downloads/webgpt/logs/trace.jsonl` (append đa tiến trình, sequence reset theo
  process start).

## 1. Stale-check — WARN (ghi nhận trung thực)

| Mốc | Giá trị |
|---|---|
| Process start-time | Wed Aug 26 **14:26:38** 2026 (PID 4105024) |
| mtime mới nhất `gpt/**` (source `.py`) | **14:49:26** — `gpt/transport/multi_account.py` (**file mới, untracked**) |
| Khác đáng chú ý | `gpt/gateway/server.py` mtime **14:46:23** (diff vs HEAD: +1092/−118); batch gần như toàn bộ `gpt/**/*.py` mtime **14:43:12** |
| Kết luận | **STALE-WARN**: có source thay đổi SAU khi process start mà uvicorn không hot-reload ⇒ process đang chạy code snapshot thời điểm 14:26:38, KHÔNG chứa các edit 14:43–14:49. pycache ghi 14:49:51 cho thấy có process khác import module sau edit. Nếu các edit này thuộc phạm vi merge cần chứng nhận thì cần 1 chu kỳ restart riêng (ngoài thẩm quyền R11) |

## 2. Run sai đích :4000 — bài học env (LOẠI khỏi dữ liệu R11)

Lần chạy đầu (`~/Downloads/verify_r11_run.log`, 15:55 stamp workdir 14:55:31): script ưu tiên
`ANTHROPIC_BASE_URL` từ shell env ⇒ bắn vào **http://127.0.0.1:4000 = `opencode2api-se` (pid 9918)**,
KHÔNG phải gateway. Kết quả ALL PASS đẹp (T1 first-token 6.526s/delta=36 · T2 exact
`HYBRID_FLIP_T2_OK` 1 round · T3 `SUM=338350` 2 rounds) — **vô giá trị làm bằng chứng R11**, đã loại.
Journal gateway không hề thấy POST nào trong khung này (khớp).

Bài học (trùng warning đã ghi trong R9, tái phát với verify agent mới): shell của agent kế thừa
`ANTHROPIC_BASE_URL=http://127.0.0.1:4000`; mọi lần chạy sau ghim cứng `--base-url http://127.0.0.1:18000`.

## 3. Ladder hợp lệ vào :18000 — lần 1 (15:00:42 → 15:01:16): FAIL toàn mức do upstream

| Mức | Kết quả | Số liệu nguyên văn |
|---|---|---|
| T1 streaming | **FAIL** (16.965s) | http=200 nhưng stream mang error event: `"Conversation failed over to a fresh ChatGPT web session; please resend this request to continue."` — delta_count=0, first_token=null. Trace `req_e526f7ab7740`: `conversation_failover {reason:"rate_limited", error_type:"RateLimited"}` → `request_completed status:"ok" http_status:200 correction_count:0`. Journal: `15:00:59 … POST /v1/messages 200 OK` |
| T2 tool_use | **FAIL** (0.073s, fast-fail) | 500: `"backend cooling down after rate limit (retry allowed in 90.0s)"` — rounds_used=0, không chạm backend |
| T3 mini-loop | **FAIL** (0.05s, fast-fail) | 500: `(retry allowed in 89.9s)` — như trên |

⇒ Breaker **trip #1**, cửa sổ 90s, deadline ≈ **15:02:46**. Turn thật duy nhất chạm upstream chính là
T1 và dính rate-limit ngay.

## 4. Chờ đúng 1 chu kỳ cooldown → retry DUY NHẤT (lần 2, 15:08:22 → 15:08:37): vẫn chặn

- Đợi quá deadline breaker (until-loop tới 15:03:10; ladder thực start 15:08:22 — muộn hơn dự kiến,
  bất lợi cho ta chứ không vi phạm luật "chờ ĐÚNG 1 chu kỳ").
- T1 **FAIL** y hệt lần 1: 200 + error event failover, 15.489s, delta_count=0. Journal:
  `15:08:22 … POST /v1/messages 200 OK`.
- T2/T3 fast-fail 500 `(retry allowed in 180.0s / 179.9s)` ⇒ breaker **trip #2**, penalty **×2 =
  180s** (deadline ≈ 15:11:40). Journal: `15:08:37 … 500 Internal Server Error` ×2.

## 5. Chốt BLOCKED-upstream

Hết ngân sách luật đặt ra (mức FAIL retry tối đa 1 lần; breaker chờ đúng 1 chu kỳ rồi thử, vẫn chặn
⇒ BLOCKED-upstream, không đốt thêm turn). Upstream rate-limit kiểu token-weighted chưa nhả sau
~8 phút từ trip #1 — cùng họ sự cố với R10 sáng nay (90→180→360s), không phải code gateway.

### Những gì stack mới ĐÃ chứng minh được dù đang blocked

1. **Refusal/failover mapping**: lỗi upstream được map thành HTTP 200 + SSE `error` event với message
   resend được — client harness parse sạch, không crash, không rác envelope.
2. **Breaker taxonomy đúng chữ ký**: trace nguyên văn `kind:"conversation_failover"
   metadata:{reason:"rate_limited", error_type:"RateLimited"}`; fast-fail 500 chỉ mất 50–73ms kèm
   countdown chính xác (90.0 → 179.9s khớp penalty ×2).
3. **correction_count = 0 trên TOÀN BỘ request R11** (trace lẫn harness): không MALFORMED storm,
   không false-completion, không correction vô nghĩa.
4. Stale-check WARN ở mục 1 là điểm cần xử lý hành chính (restart chu kỳ mới nếu muốn chạy code
   14:43–14:49), không phải lỗi runtime.

### So sánh baseline R9 (không đối chiếu trực tiếp được — không có turn thành công nào)

| Mức | R9 (25/08) | R11 (26/08) |
|---|---|---|
| T1 streaming | PASS, first_token **13.473s**, delta=6 | n/a — rate-limited trước token đầu |
| T2 tool_use | PASS, exact `HYBRID_FLIP_T2_OK`, corr=0 | n/a — fast-fail qua breaker |
| T3 mini-loop | PASS, **SUM=338350**, rounds=4 | n/a — fast-fail qua breaker |

(Run :4000 đạt cả 3 mốc này trên bridge khác — tham khảo thôi, không tính.)

## 6. Khuyến nghị

1. Coordinator: đợi ít nhất đến sau deadline trip #2 (~15:11:40, an toàn hơn: +1 chu kỳ nữa ≈ 600s
   trần) rồi bắn ĐÚNG 1 gate probe T1 nhỏ trước mọi batch việc; probe pass mới chạy ladder/CTF.
2. Xử lý stale-check: quyết định có restart để nạp code 14:43–14:49 hay giữ process hiện tại làm
   baseline; ghi vào DECISIONS.md nếu restart.
3. Harness: cân nhắc hard-pin base-url hoặc cảnh báo khi `ANTHROPIC_BASE_URL` env ≠ default gateway
   (tái phát 2 lần liên tiếp R9, R11).

— Báo cáo bởi verify agent R11, chốt 15:13 +0700 2026-08-26.
